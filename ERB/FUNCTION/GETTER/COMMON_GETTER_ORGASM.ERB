;==================================================
;
;	絶頂系参照関数置き場
;	
;==================================================
;-------------------------------------------------
;関数名:_SHOOTING
;概　要:射精種から文字列を取得する関数
;引　数:ARG…(TCVAR:射精)
;戻り値:射精種を表す文字列
;備　考:式中関数。内部関数としての使用を想定
;-------------------------------------------------
@_SHOOTING(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "なし/空射精/射精/大量射精/早漏/前立腺射精", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;関数名:GET_SHOOTING
;概　要:ARGの射精種を取得する
;;引　数:ARG…キャラ登録番号。省略するとTARGET
;戻り値:射精種を表す文字列
;備　考:式中関数
;-------------------------------------------------
@GET_SHOOTING(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _SHOOTING(TCVAR:ARG:射精)

;-------------------------------------------------
;関数名:SHOOTING
;概　要:ARGSが指定した射精種なら真を返す
;引　数:ARG…キャラ番号
;引　数:ARGS…射精種
;戻り値:その状態なら1、違えば0
;備　考:式中関数
;-------------------------------------------------
@SHOOTING(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _SHOOTING(TCVAR:ARG:射精)

;単純にMILKで良い気がしたけどどこかで被りそうな気もしたのでSPLAYING!
;-------------------------------------------------
;関数名:_SPLAYING_MILK
;概　要:噴乳種から文字列を取得する関数
;引　数:ARG…(TCVAR:噴乳)
;戻り値:射精種を表す文字列
;備　考:式中関数。内部関数としての使用を想定
;-------------------------------------------------
@_SPLAYING_MILK(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "なし/空噴乳/噴乳/大量噴乳", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;関数名:GET_SPLAYING_MILK
;概　要:ARGの噴乳種を取得する
;;引　数:ARG…キャラ登録番号。省略するとTARGET
;戻り値:噴乳種を表す文字列
;備　考:式中関数
;-------------------------------------------------
@GET_SPLAYING_MILK(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _SHOOTING(TCVAR:ARG:噴乳)

;-------------------------------------------------
;関数名:SPLAYING_MILK
;概　要:ARGSが指定した噴乳種なら真を返す
;引　数:ARG…キャラ番号
;引　数:ARGS…噴乳種
;戻り値:その状態なら1、違えば0
;備　考:式中関数
;-------------------------------------------------
@SPLAYING_MILK(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _SPLAYING_MILK(TCVAR:ARG:噴乳)

;FEMALE_EJACULATIONとかSHIOFUKI等は論外だと思われるのでSQUIRTING!
;-------------------------------------------------
;関数名:_SQUIRTING
;概　要:潮吹きから文字列を取得する関数
;引　数:ARG…(TCVAR:潮吹き)
;戻り値:潮吹き種を表す文字列
;備　考:式中関数。内部関数としての使用を想定
;-------------------------------------------------
@_SQUIRTING(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "なし/潮吹き/大量潮吹き", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;関数名:GET_SQUIRTING
;概　要:ARGの潮吹き種を取得する
;;引　数:ARG…キャラ登録番号。省略するとTARGET
;戻り値:潮吹き種を表す文字列
;備　考:式中関数
;-------------------------------------------------
@GET_SQUIRTING(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _SQUIRTING(NOWEX:ARG:潮吹き)

;-------------------------------------------------
;関数名:SQUIRTING
;概　要:ARGSが指定した潮吹き種なら真を返す
;引　数:ARG…キャラ番号
;引　数:ARGS…潮吹き種
;戻り値:その状態なら1、違えば0
;備　考:式中関数
;-------------------------------------------------
@SQUIRTING(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _SQUIRTING(TCVAR:ARG:潮吹き)

;-------------------------------------------------
;関数名:_DRY_CUM
;概　要:空射精種から文字列を取得する関数
;引　数:ARG…(TCVAR:空射精)
;戻り値:空射精種を表す文字列
;備　考:式中関数。内部関数としての使用を想定
;-------------------------------------------------
@_DRY_CUM(ARG)
#FUNCTIONS
#LOCALSSIZE 10
SIF !STRLENS(LOCALS)
	SPLIT "なし/空射精/前立腺", "/", LOCALS, LOCAL
RETURNF LOCALS:ARG

;-------------------------------------------------
;関数名:GET_DRY_CUM
;概　要:ARGの空射精種を取得する
;;引　数:ARG…キャラ登録番号。省略するとTARGET
;戻り値:空射精種を表す文字列
;備　考:式中関数
;-------------------------------------------------
@GET_DRY_CUM(ARG = -2)
#FUNCTIONS
ARG = ARG != -2 ? ARG # TARGET
RETURNF _DRY_CUM(TCVAR:ARG:空射精)

;-------------------------------------------------
;関数名:DRY_CUM
;概　要:ARGSが指定した空射精種なら真を返す
;引　数:ARG…キャラ番号
;引　数:ARGS…空射精種
;戻り値:その状態なら1、違えば0
;備　考:式中関数
;-------------------------------------------------
@DRY_CUM(ARG = -2, ARGS)
#FUNCTION
ARG = ARG != -2 ? ARG # TARGET
RETURNF ARGS == _DRY_CUM(TCVAR:ARG:空射精)

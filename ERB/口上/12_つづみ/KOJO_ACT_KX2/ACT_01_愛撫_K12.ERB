;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	すずきつづみ口上 愛撫系アクション口上ファイル
;
;	愛撫系アクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;愛撫系 ACT10～19 & 27
;========================================================================
;・TFLAG:ACT(10=手で愛撫/11=胸愛撫/12=口で愛撫/13=アナル愛撫/14=アナル舐め/15=キス/16=性器を擦り合う/17=パイズリする/18=足コキする/19=指挿入れ/27=押し倒す)
;・TFLAG:ACT分類 == 1に該当
;・ACT18, 足コキは異常系から派生する場合あり
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "愛撫")で拾う事が可能
;・調教者が調教対象を愛撫する。この口上の時点で調教者はすでに手を出している
;・媚薬を使用するケースがある
;-------------------------------------------------
;・戻り値(RESULT)について
;・システム側では口上側のRESULT(終端のRETURN 1など)によって口上が表示されたのか否か、
;・また箇所毎の特有の挙動(媚薬等口上をスキップなど)を制御しています
;・@口上記入チェックの場合代入処理を省略できますが、RESULT値は口上側で決めうちする必要があります
;・特別な理由がない限り、テンプレとしてはRETURN 1を記述しておきます
;-------------------------------------------------

;────────────────────────────────────
;ACT10, 手で愛撫(ACT10_0, 愛撫する/ACT10_1, 手淫する/ACT10_2, ペニスバンド手淫)
;────────────────────────────────────
;・Ｃ中心に愛撫する
;・調教対象にペニスがある場合、及びペニスバンド装着時には派生コマンドになる
;・調教対象が自慰中の場合は解除される
;・調教者の手に汚れがある場合不快SOURCEが変動する
;・調教者技巧、愛撫、器用な指先素質に応じて勃起度上昇
;-------------------------------------------------
@KOJO_ACT_K12_10
;-------------------------------------------------
;・TFLAG:ACT派生(0=愛撫/1=手淫/2=ペニスバンド手淫)
;-------------------------------------------------

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_1, 手淫する
;━━━━━━━━━━━━━━━━━━━━━━━━
;・男性器への愛撫を行う、つまり手コキ
;・他の派生と比べてSOURCE:MASTER:快Ｃは+200%
;・調教者技巧、愛撫、器用な指先素質に応じて勃起度上昇
;-------------------------------------------------
;必要に応じて、TEQUIP:オナホールやTCVAR:MASTER:勃起度
;PANTSCHECK(1)等で分岐を設ける事もできます。
;まともにやると手間が尋常じゃないので、効率よく記述する事をお勧めします
;-------------------------------------------------
IF 口上記入チェック(100) && TFLAG:ACT派生 == 1
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教内初回
	;=================================================
	IF 口上記入チェック(0) && ONCEACT(0)
		;下半身服装がある場合
		IF PANTSCHECK(1)
			PRINTFORMW 
		;その他
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;調教対象がイキそう
	;=================================================
	IF 口上記入チェック(100) && TCVAR:MASTER:イきそう
		;-------------------------------------------------
		;前から愛撫
		;-------------------------------------------------
		IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 1
			;焦らし度によって分岐
			SELECTCASE TCVAR:MASTER:焦らし度
				;焦らし中
				CASE IS > 1
				;焦らし終了
				CASE 1
					PRINTFORMW 
				;焦らし無し
				CASEELSE
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;後ろから愛撫
		;-------------------------------------------------
		IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 2
			;焦らし度によって分岐
			SELECTCASE TCVAR:MASTER:焦らし度
				;焦らし中
				CASE IS > 1
					PRINTFORMW 
				;焦らし終了
				CASE 1
					PRINTFORMW 
				;焦らし無し
				CASEELSE
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
		
		;位置前後分岐を埋めるなら必要なし。埋めないならここで補完させましょう
		;不要な分岐は削除、もしくはコメントアウトすることで負荷は軽くなります
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(100)
			IF TCVAR:MASTER:勃起度 < 10
			ELSE
				;焦らし度によって分岐
				SELECTCASE TCVAR:MASTER:焦らし度
					;焦らし中
					CASE IS > 1
						SELECTCASE RAND:5
							CASE 0
								CALL PRINTFORMW2T , "「射精したい、したい、射精したい」"
								CALL PRINTFORMW2T , "「そんなおちんちんの切ない願いが聞こえるわ」"
								CALL PRINTFORMW2T , "「でも駄目。射精は許さない」"
								CALL PRINTFORMW2T , "「十分に精液が生産されて、陰茎にどっぷりと溜まるまで、耐えなさい」"
							CASE 1
								CALL PRINTFORMW2T , "「亀頭だけ刺激することで、射精を抑制して快楽のみ与え続けられるわ」"
								CALL PRINTFORMW2T , "「ぬるぬるして、ぞわぞわして……あっ、びくっとしたわね」"
							CASE 2
								CALL PRINTFORMW2T , "「カウパーがどろどろと出ているわね」"
								CALL PRINTFORMW2T , "「その調子よ、しっかり刺激に耐えなさい」"
							CASE 3
								CALL PRINTFORMW2T , "「おちんちんがずっしりとしてパンパンになってるわ」"
								CALL PRINTFORMW2T , "「苦しいのよね？でもまだ駄目。射精してはいけない」"
							CASEELSE
								CALL PRINTFORMW2T , "「私、この射精寸前のおちんちんが一番好きよ」"
								CALL PRINTFORMW2T , "「射精したい。おちんちんから白いもの出したい。」"
								CALL PRINTFORMW2T , "「そういってビクビクするおちんちんが一番かわいいんですもの」"
						ENDSELECT
					;焦らし終了
					CASE 1
						CALL PRINTFORMW2T , "「もう十分そうね」"
						CALL PRINTFORMW2T , "「いいわ、射精してしまいなさい」"
						CALL PRINTFORMW2T , "「おちんちんの奥から、白くて濃いザーメンをいっぱい出しなさい」"
					;焦らし無し
					CASEELSE
						CALL PRINTFORMW2T , "「もうイキそうなのね」"
				ENDSELECT
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(100) && TFLAG:ACT == TFLAG:前ACT
		;-------------------------------------------------
		;顔面騎乗中
		;-------------------------------------------------
		IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
			;前から愛撫
			IF TCVAR:MASTER:位置前後 == 1
				PRINTFORMW 
			;後ろから愛撫
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(100)

				CALL PRINTFORMW2T , "「まだ刺激が足りないようね」"
				CALL PRINTFORMW2T , "「でもおちんちんを触られたことで、あなたの勃起したおちんちんに精子が上がってくる」"
				CALL PRINTFORMW2T , "「睾丸、根本、尿道、ぞくぞくとザーメンが上がってくるわ」"
			RETURN 1
		ENDIF
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(100)
		IF TCVAR:MASTER:勃起度
				CALL PRINTFORMW2T , "「射精は気持ちがいいものよ」"
				CALL PRINTFORMW2T , "「性欲が高まって、快感が一気に放出されて、おちんちんが幸せになるの」"
				CALL PRINTFORMW2T , "「あなたのおちんちんも幸せにしてあげる」"
		ELSE
				CALL PRINTFORMW2T , "「あら、まだ小さいままなのね」"
				PRINTFORMDW つづみはまだ勃起していないあなたの陰茎をふにふにと触り始める
				CALL PRINTFORMW2T , "「想像して。勃起したおちんちんを」"
				CALL PRINTFORMW2T , "「私に弄られて、射精を求めてパンパンに腫れたおちんちん」"
				CALL PRINTFORMW2T , "「からだじゅうの血流と感覚が勃起したおちんちんに集中するの」"
				CALL PRINTFORMW2T , "「おちんちん。おちんちん。考えるのはおちんちんのことだけ」"
				PRINTFORMDW 言葉につい反応してしまい、股間のイチモツはむくむくと奮い立っていく
				CALL PRINTFORMW2T , "「はい。上出来よ」"
		ENDIF
		
		RETURN 1
	ENDIF
ENDIF
;ACT10_1, 手淫する

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_2, ペニスバンド手淫
;━━━━━━━━━━━━━━━━━━━━━━━━
;・調教対象がペニスバンド装着時に発生
;・快ＣSOURCE的にはTFLAG:ACT派生 == 1か否かでしか見てないっぽいので、事実上手淫ではなく愛撫と同等？
;・快Ｖも見てないような感じ。そして部位の汚れも参照できないんじゃないかなと
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTACT(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教内初回
	;=================================================
	IF 口上記入チェック(0) && ONCEACT(1)
		;下半身服装がある場合
		IF PANTSCHECK(1)
			PRINTFORMW 
		;その他
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT == TFLAG:前ACT
		;-------------------------------------------------
		;顔面騎乗中
		;-------------------------------------------------
		IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
			;前から愛撫
			IF TCVAR:MASTER:位置前後 == 1
				PRINTFORMW 
			;後ろから愛撫
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			;前から愛撫
			IF TCVAR:MASTER:位置前後 == 1
				PRINTFORMW 
			;後ろから愛撫
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		;前から愛撫
		IF TCVAR:MASTER:位置前後 == 1
			PRINTFORMW 
		;後ろから愛撫
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;ACT10_2, ペニスバンド手淫

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT10_0, 愛撫する
;━━━━━━━━━━━━━━━━━━━━━━━━
;・女性器への愛撫を行う
;・SOURCE的には快Ｂと快Ｃは同等
;-------------------------------------------------
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTACT(2)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教内初回
	;=================================================
	IF 口上記入チェック(0) && ONCEACT(2)
		;下半身服装がある場合
		IF PANTSCHECK(1)
			PRINTFORMW 
		;その他
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;調教対象がイキそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		;-------------------------------------------------
		;前から愛撫
		;-------------------------------------------------
		IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 1
			;焦らし度によって分岐
			SELECTCASE TCVAR:MASTER:焦らし度
				;焦らし中
				CASE IS > 1
					PRINTFORMW 
				;焦らし終了
				CASE 1
					PRINTFORMW 
				;焦らし無し
				CASEELSE
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;後ろから愛撫
		;-------------------------------------------------
		IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 2
			;焦らし度によって分岐
			SELECTCASE TCVAR:MASTER:焦らし度
				;焦らし中
				CASE IS > 1
					PRINTFORMW 
				;焦らし終了
				CASE 1
					PRINTFORMW 
				;焦らし無し
				CASEELSE
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
		
		;位置前後分岐を埋めるなら必要なし。埋めないならここで補完させましょう
		;不要な分岐は削除、もしくはコメントアウトすることで負荷は軽くなります
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			;焦らし度によって分岐
			SELECTCASE TCVAR:MASTER:焦らし度
				;焦らし中
				CASE IS > 1
					PRINTFORMW 
				;焦らし終了
				CASE 1
					PRINTFORMW 
				;焦らし無し
				CASEELSE
					PRINTFORMW 
			ENDSELECT
			RETURN 1
		ENDIF
	ENDIF
	
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT == TFLAG:前ACT
		;-------------------------------------------------
		;顔面騎乗中
		;-------------------------------------------------
		IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
			;前から愛撫
			IF TCVAR:MASTER:位置前後 == 1
				PRINTFORMW 
			;後ろから愛撫
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
		
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			;前から愛撫
			IF TCVAR:MASTER:位置前後 == 1
				PRINTFORMW 
			;後ろから愛撫
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN 1
		ENDIF
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		;前から愛撫
		IF TCVAR:MASTER:位置前後 == 1
			PRINTFORMW 
		;後ろから愛撫
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF
;ACT10_0, 愛撫する

;────────────────────────────────────
;ACT11, 胸愛撫
;────────────────────────────────────
;・胸愛撫/乳首吸い/乳の揉み合い/乳首責め
;・特定の条件で「乳首吸い」「乳の揉み合い」「乳首責め」へ派生する
;・調教対象がオトコの場合は必ず「乳首責め」になり、他に派生する事はない
;-------------------------------------------------
@KOJO_ACT_K12_11
;-------------------------------------------------
;・TFLAG:ACT派生(0=胸愛撫/1=乳首吸い/2=揉み合い/3=乳首責め)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_0, 胸愛撫
;━━━━━━━━━━━━━━━━━━━━━━━━
;・全ての派生条件から漏れると通常の胸愛撫になる
;・調教者の欲望+Ｂ感覚が3以上の場合1/2の確率で揉み合いに派生
;・それから漏れた場合にのみ、通常の胸愛撫となる
;-------------------------------------------------
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;前から愛撫
	IF TCVAR:MASTER:位置前後 == 1
		PRINTFORMW 
	;後ろから愛撫
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_1, 乳首吸い
;━━━━━━━━━━━━━━━━━━━━━━━━
;・前回ACTが胸愛撫の場合乳首系になる
;・その際、調教者に幼稚か舌使いがあると3/4、なければ1/4で乳首吸いとなる
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;上半身服装が要押し退け			服をまくりあげて
	IF BRACHECK(1, 1)
		PRINTFORMW
	;その他
	ELSE
		PRINTFORMW
	ENDIF
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_2, 乳の揉み合い
;━━━━━━━━━━━━━━━━━━━━━━━━
;・胸の揉み合いを要求する。この時点での調教対象の反応は未定
;・乳首系の条件から漏れた場合、調教者の欲望+Ｂ感覚３以上なら、1/2で胸の揉み合いになる
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;=================================================
	;上半身服装が邪魔			服の上から
	;=================================================
	IF 口上記入チェック(0) && BRACHECK(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;上半身上着が要押し退け		上着をまくり上げ
	;=================================================
	IF 口上記入チェック(0) && UPPERJACKET(1) == 1
		;下着もまくり上げた
		IF UPPERUNDER(1) == 1
			PRINTFORMW 
		;その他
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;上半身上着が要押し退け		下着の裾から手を忍ばせ
	;=================================================
	IF 口上記入チェック(0) && UPPERUNDER(1) == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT11_3, 乳首責め
;━━━━━━━━━━━━━━━━━━━━━━━━
;・前回ACTが胸愛撫の場合乳首系になる
;・その際、調教者に幼稚か舌使いがあると1/4、なければ3/4で乳首責めとなる
;・調教対象がオトコの場合は乳首責めにしかならない
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
	;=================================================
	;調教対象がオトコ
	;=================================================
	IF 口上記入チェック(0) && TALENT:MASTER:オトコ
		;前から愛撫
		IF TCVAR:MASTER:位置前後 == 1
			PRINTFORMW 
		;後ろから愛撫
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		;前から愛撫
		IF TCVAR:MASTER:位置前後 == 1
			PRINTFORMW 
		;後ろから愛撫
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT12, 口で愛撫
;────────────────────────────────────
;・クンニする/フェラチオする
;・下半身の衣装が残っていても発生することがある。調教者にTALENT:壁抜けがない場合はずらして実行する
;・ターン方針が"異常"なら発生しない
;・TEQUIP(バイブ/オナホール/ペニスバンド/三角木馬)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・調教対象にペニスがあると、必ずフェラになる
;-------------------------------------------------
@KOJO_ACT_K12_12
;-------------------------------------------------
;・TFLAG:ACT派生(0=クンニ/1=フェラ)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT12_0, クンニする
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;顔面騎乗、アライメントが負の場合、TALENT:サド等で地の文が分岐する場合あり
	;必要に応じて拡張すると良いでしょう
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		;焦らし度派生処理
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1 ;焦らし中
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;焦らし無し
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT == TFLAG:前ACT
		;-------------------------------------------------
		;顔面騎乗中
		;-------------------------------------------------
		IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;連続実行のネストを開放するならここは必ず書いておきましょう
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;ここを最初に書きましょう
	;LOWERJACKET(1) == 1「まくりあげて」LOWERUNDER(1) == 1「ずらして」等で拡張するのも良いでしょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT12_1, フェラチオする
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;顔面騎乗、アライメントが負の場合、TALENT:サド等で地の文が分岐する場合あり
	;必要に応じて拡張すると良いでしょう
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		;焦らし度派生処理
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;焦らし無し
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT == TFLAG:前ACT
		;-------------------------------------------------
		;顔面騎乗中
		;-------------------------------------------------
		IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
			PRINTFORMW 
			RETURN 1
		ENDIF
		
		;連続実行のネストを開放するならここは必ず書いておきましょう
		;-------------------------------------------------
		;その他
		;-------------------------------------------------
		IF 口上記入チェック(0)
			PRINTFORMW 
			RETURN 1
		ENDIF
	ENDIF
	
	;ここを最初に書きましょう
	;LOWERJACKET(1) == 1「まくりあげて」LOWERUNDER(1) == 1「ずらして」等で拡張するのも良いでしょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT13, アナル愛撫
;────────────────────────────────────
;・アナル愛撫/前立腺刺激/アナルバイブ出し入れ
;・アナルバイブ装備中は、アナルバイブ出し入れに派生
;・連続実行の場合、前立腺刺激に派生
;・イきそうor快Ｃが5000以上の場合、調教者の愛撫スキルに応じて前立腺刺激に派生
;・勃起度が低い場合、調教者の愛撫スキルに応じて前立腺刺激に派生
;・要強制勃起の場合、前立腺刺激に派生
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"ソフト", "異常"だと発生しない
;・TEQUIP(三角木馬/ペニスバンド/アナルビーズ)装着中には発生しない
;・性交奉仕中には発生しない
;-------------------------------------------------
@KOJO_ACT_K12_13
;-------------------------------------------------
;・TFLAG:ACT派生（0=アナル愛撫/1=前立腺刺激/2=アナルバイブ出し入れ）
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_0, アナル愛撫
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;顔面騎乗中
	;=================================================
	IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;下半身服装が要押し退け
	;=================================================
	IF 口上記入チェック(0) && PANTSCHECK(1, 1)
		;下半身上着をまくりあげ
		IF LOWERJACKET(1) == 1
			PRINTFORMW 
		;下着越しに
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_1, 前立腺刺激(アナル愛撫)
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象がオトコの場合のみに派生
;ふたなりの前立腺は尿道、つまり膣の上にあるとし、Ｇスポット刺激に
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT13_2, アナルバイブ出し入れ
;━━━━━━━━━━━━━━━━━━━━━━━━
;・アナルバイブ装備中は、アナルバイブ出し入れになる
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;調教対象がオトコ
	IF TALENT:MASTER:オトコ
		PRINTFORMW 
	;その他
	ELSE
		PRINTFORMW 
	ENDIF
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT14, アナル舐め
;────────────────────────────────────
;・アナル舐め
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"ソフト", "異常"だと発生しない
;・TEQUIP(アナルバイブ/アナルビーズ/浣腸器＋プラグ/ペニスバンド/三角木馬)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・TCVAR:MASTER:焦らし度が真になっている場合は発生しない
;・舌による前立腺刺激への派生は「そんなところまで舌が届くわけないじゃないかｗｗｗ」という事で粛清されています
;・現時点でACTが派生する事はないが、ACT改変の可能性に備え、TFLAG:ACT派生 == 0で指定
;-------------------------------------------------
@KOJO_ACT_K12_14
;-------------------------------------------------
;・ACT派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT14_0, アナル舐め
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;初回
	;=================================================
	IF 口上記入チェック(0) && FIRSTACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教内初回
	;=================================================
	IF 口上記入チェック(0) && ONCEACT(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教者が自慰中
	;=================================================
	IF 口上記入チェック(0) && GETBIT(TEQUIP:自慰中, 1)
		;調教者がペニスバンド身に付けている
		IF TEQUIP:調教者ペニスバンド
			PRINTFORMW
		;その他
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;=================================================
	;顔面騎乗中
	;=================================================
	IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教対象が自慰中
	;=================================================
	IF 口上記入チェック(0) && GETBIT(TEQUIP:自慰中, 0)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;下半身服装が要押し退け
	;=================================================
	IF 口上記入チェック(0) && PANTSCHECK(1, 1)
		;下半身上着をまくりあげ
		IF LOWERJACKET(1) == 1
			PRINTFORMW 
		;下着越しに
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT15, キス
;────────────────────────────────────
;・キス/媚薬キス
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"ハード", "異常"だと発生しない
;・調教者にTALENT:壁抜けが無い場合、TEQUIP:ボールギャグ装着中は弾かれる
;-------------------------------------------------
@KOJO_ACT_K12_15
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15, キス 初回
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && FIRSTACT(0)
	;=================================================
	;お互いに初めて
	;=================================================
	IF 口上記入チェック(0) && EXP:MASTER:キス経験 == 0 && EXP:TARGET:キス経験 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教対象が初めて
	;=================================================
	IF 口上記入チェック(0) && EXP:MASTER:キス経験 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;調教者が初めて
	;=================================================
	IF 口上記入チェック(0) && EXP:TARGET:キス経験 == 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;"初回"のネストを開放し、各条件の分岐を放置するならここで補完しましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15_0, キス
;━━━━━━━━━━━━━━━━━━━━━━━━
;!GETBIT(TFLAG:媚薬等使用, 1)等の条件を削除して!TFLAG:ACT派生でまとめてしまっても良いと思います
;無理に全ての箇所を埋める事はないので、必要に応じて使い分けて下さい
;-------------------------------------------------
IF 口上記入チェック(0) && !TFLAG:ACT派生 && !GETBIT(TFLAG:媚薬等使用, 1)
	;=================================================
	;性交中（調教者挿入）
	;=================================================
	IF 口上記入チェック(0) && TEQUIP:性交中
		;体位を派生処理
		SELECTCASE TEQUIP:性交中
			;正常位					頭をつかんで情熱的なキスをした…
			CASE 1
				PRINTFORMW 
			;後背位					振り向かせ唇を奪った…
			CASE 2
				PRINTFORMW 
			;騎乗位					下から突き上げながら情熱的に口づけした…
			CASE 3
				PRINTFORMW 
			;対面座位				ぎゅっと抱きしめるとそのまま唇を奪った…
			CASE 4
				PRINTFORMW 
			;背面座位				振り向かせそのまま唇を奪った…
			CASE 5
				PRINTFORMW 
			;アナルセックス			アヌスを犯しながらキスしてきた…
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;性交奉仕中（調教対象挿入）
	;=================================================
	IF 口上記入チェック(0) && TEQUIP:性交奉仕中
		;焦らし度による派生			TCVAR:MASTER:イきそうで囲んではいないので、CASEELSEは通常の状態を意味する
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;焦らし無し
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT15_1, 媚薬キス
;━━━━━━━━━━━━━━━━━━━━━━━━
;・調教対象に口移しで媚薬を飲ませる
;-------------------------------------------------
;RESULT値を2として@KOJO_USE_KXの媚薬等使用口上を呼び出さない設定をデフォルトとしておきます
;必要に応じて戻り値を設定してください
;-------------------------------------------------
IF 口上記入チェック(0) && !TFLAG:ACT派生 && GETBIT(TFLAG:媚薬等使用, 1)
	;媚薬使用口上処理済チェック(媚薬使用の地の文カットする際はTFLAG:媚薬等使用地の文スキップのコメントを解除)
	CLEARBIT TFLAG:媚薬等使用口上, 1
	;CLEARBIT TFLAG:媚薬等使用地の文スキップ, 1
	;=================================================
	;性交中（調教者挿入）
	;=================================================
	IF 口上記入チェック(0) && TEQUIP:性交中
		;体位を派生処理
		SELECTCASE TEQUIP:性交中
			;正常位					頭をつかんで情熱的なキスをした…
			CASE 1
				PRINTFORMW 
			;後背位					振り向かせ唇を奪った…
			CASE 2
				PRINTFORMW 
			;騎乗位					下から突き上げながら情熱的に口づけした…
			CASE 3
				PRINTFORMW 
			;対面座位				ぎゅっと抱きしめるとそのまま唇を奪った…
			CASE 4
				PRINTFORMW 
			;背面座位				振り向かせそのまま唇を奪った…
			CASE 5
				PRINTFORMW 
			;アナルセックス			アヌスを犯しながらキスしてきた…
			CASE 6
				PRINTFORMW 
		ENDSELECT
		RETURN 2
	ENDIF
	
	;=================================================
	;性交奉仕中（調教対象挿入）
	;=================================================
	IF 口上記入チェック(0) && TEQUIP:性交奉仕中
		;焦らし度による派生			TCVAR:MASTER:イきそうで囲んではいないので、CASEELSEは通常の状態を意味する
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;焦らし無し
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 2
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 2
	ENDIF
ENDIF

;────────────────────────────────────
;ACT16, 性器を擦り合う
;────────────────────────────────────
;・貝合わせ/素股する
;・調教対象にペニスがある場合はACT派生 == 1「素股する」へ派生する
;・調教対象がペニスバンド装着中は、ACT派生 == 2「ペニスバンド素股」になる
;・ターン方針が"ハード", "異常だと発生しない
;・調教対象が女の子でバイブ装着中だと発生しない
;・TEQUIP(オナホール/三角木馬)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・調教者にペニスがあると発生しない（奉仕系の素股強制になる）
;-------------------------------------------------
@KOJO_ACT_K12_16
;-------------------------------------------------
;・TFLAG:ACT派生（=0,貝あわせ/=1,素股/=2,ペニスバンド素股）
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_0, 貝合わせ
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		;焦らし度派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;焦らし無し
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT == TFLAG:前ACT
		;仰向け
		IF TFLAG:仰向け
			PRINTFORMW
		;その他
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;地の文は調教対象の下半身服装毎の分岐あり。必要に応じて拡張してください
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		;仰向け
		IF TFLAG:仰向け
			PRINTFORMW
		;その他
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_1, 素股する
;━━━━━━━━━━━━━━━━━━━━━━━━
;・女性器と太股で男性器を挟んで愛撫する
;・調教対象にペニスがあると、必ず素股するになる
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		;焦らし度派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;焦らし無し
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT == TFLAG:前ACT
		;仰向け
		IF TFLAG:仰向け
			PRINTFORMW
		;その他
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;地の文は調教対象の下半身服装毎の分岐あり。必要に応じて拡張してください
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		;仰向け
		IF TFLAG:仰向け
			PRINTFORMW
		;その他
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT16_2, ペニスバンド素股
;━━━━━━━━━━━━━━━━━━━━━━━━
;・調教対象がペニスバンド装着中は、ペニスバンド素股になる
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT == TFLAG:前ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;地の文は調教対象の下半身服装毎の分岐あり。必要に応じて拡張してください
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		;仰向け
		IF TFLAG:仰向け
			PRINTFORMW
		;その他
		ELSE
			PRINTFORMW
		ENDIF
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT17, パイズリする
;────────────────────────────────────
;・TFLAG:お仕置きフラグがたっていると発生しない
;・ターン方針が"ハード", "異常"の場合は発生しない
;・TEQUIP(オナホール/三角木馬)装着中には発生しない
;・調教対象にペニスがない場合は発生しない
;・調教者がオトコの場合は発生しない
;・現在ACT派生は存在しないが将来の改変に備え、ACT派生 == 0とする
;-------------------------------------------------
@KOJO_ACT_K12_17
;-------------------------------------------------
;・ACT派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT17_0, パイズリする
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;地の文は調教者の胸のサイズで分岐します。
	;素質を変更する事が可能なので、必要に応じて拡張するのも良いでしょう
	;=================================================
	;調教対象がイキそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		;焦らし度によって分岐
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;焦らし無し
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;=================================================
	;連続実行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT == TFLAG:前ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		[SKIPSTART]
		;---------------------------------------------------
		;地の文には着衣の状態による分岐が存在しますが、書き分けも面倒なので必要に応じてコメントアウトを外すなり
		;望む形に改変するなりして使用して下さい
		;---------------------------------------------------
		;干渉する衣服が存在する時
		IF TRUECHECK(TEQUIP:上半身下着, TEQUIP:上半身上着, TEQUIP:全身上着) > 0 || (TEQUIP:MASTER:下半身下着 && !TALENT:壁抜け) || CFLAG:衣装 == 3
			;露出状況により分岐
			SELECTCASE TEQUIP:着衣露出 & 6
				;干渉箇所全露出時
				CASE 6
					PRINTFORMW 
				;調教対象性器露出時
				CASE 4
					PRINTFORMW 
				;調教者胸露出時
				CASE 2
					PRINTFORMW 
				;干渉箇所非露出時
				CASEELSE
					PRINTFORMW 
			ENDSELECT
		;その他
		ELSE
			PRINTFORMW 
		ENDIF
		[SKIPEND]
		;他に分岐を設けないのなら、ここは書いておきましょう
		;分岐を設けるならここのPRINTは空行を生むので削除しましょう
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT18, 足コキする
;────────────────────────────────────
;・足コキする
;・調教対象にペニスがない場合は発生しない
;・異常系から派生する場合もある
;・TFLAG:お仕置きフラグがたっていると発生しない
;・今日の方針が"イチャイチャ"だと発生しない
;・TEQUIP(オナホール/三角木馬)装着中は発生しない
;・性交中/性交奉仕中は発生しない
;・調教者に足が無いと発生しない
;・調教対象が女の子だと発生しない
;・現在ACT派生は存在しないが将来の改変に備え、ACT派生 == 0とする
;-------------------------------------------------
@KOJO_ACT_K12_18
;-------------------------------------------------
;・ACT派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT18, 足コキする
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;地の文にTCVAR:MASTER:焦らし度等の分岐は存在しない箇所
	;=================================================
	;調教対象がイキそう				両足で%CALLNAME:MASTER%のペニスを荒々しくしごいてきた…
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;足コキは作者の趣味ではないようで、地の文は割りと手抜きだったり
	;逆に言えば、色々とやっても地の文との整合性が崩れる心配はあまりないので、好きなようにやると良いでしょう
	;=================================================
	;連続実行						愉快そうに%CALLNAME:MASTER%を見下ろしながらペニスをぐりぐりと踏みつけている…
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT == TFLAG:前ACT
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;地の文に派生は設けてありませんが、これは割りと必要と思われるので
	;=================================================
	;靴下履いたまま足コキ(デフォルトで靴下履いていないキャラは表示されない)
	;=================================================
	IF 口上記入チェック(0) && TEQUIP:靴下
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;調教者にTALENT:壁抜けがない場合、布越しになる
	;=================================================
	;下半身服装が邪魔
	;=================================================
	IF 口上記入チェック(0) && PANTSCHECK(1)
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT19, 指挿入れ(ACT19_0, 指挿入れ/ACT19_1, Ｇスポット刺激/ACT19_2, バイブ出し入れ)
;────────────────────────────────────
;・調教対象がふたなり若しくはオンナの場合に発生
;・調教対象が処女の場合Ｇスポット刺激等に派生しない＆快Ｖにマイナス補正
;・バイブ装備中はバイブ出し入れに派生
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"異常"だと発生しない
;・TEQUIP(三角木馬/ペニスバンド)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・調教対象がオトコだと発生しない
;・調教対象がふたなりの場合、焦らし中には発生しない
;-------------------------------------------------
;※調教対象が処女ではない
;※連続実行の場合
;※イきそうor快Ｃが5000以上の場合、調教者の愛撫スキルに応じて
;※勃起度が低い場合、調教者の愛撫スキルに応じて
;※要強制勃起の場合
;・上記条件の場合Ｇスポット刺激に派生する
;-------------------------------------------------
@KOJO_ACT_K12_19
;-------------------------------------------------
;・TFLAG:ACT派生(0=指挿入れ/1=Ｇスポット刺激/2=バイブ出し入れ)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT19_0, 指挿入れ					膣口に指先をあてがい、そっと奥へ押し込んだ…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && !TFLAG:ACT派生
	;=================================================
	;顔面騎乗中
	;=================================================
	IF 口上記入チェック(0) && TRUECHECK(TEQUIP:顔面騎乗, TEQUIP:顔面騎乗アナル) > 0
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;位置前後分岐は必要に応じて
	;何処かで統合してしまうのも良いと思います
	;=================================================
	;後ろから
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;前から
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT19_1, Ｇスポット刺激					指を進入させＧスポットを直接刺激した…
;━━━━━━━━━━━━━━━━━━━━━━━━
;イきそうor快Ｃが5000以上の場合、調教者の愛撫スキルに応じてＧスポット刺激
;Ｇスポット責めに派生すると前立腺刺激扱いになる
;-------------------------------------------------
;調教対象がふたなりの場合でも発生するので、必要に応じてPENIS(MASTER)なり!PENIS(MASTER)で分岐を設けると良いと思います
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	;=================================================
	;後ろから
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;前から
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:位置前後 == 1
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT19_2, バイブ出し入れ					Ｇスポットを抉るように激しく動かした…
;━━━━━━━━━━━━━━━━━━━━━━━━
;地の文自体に派生はありませんが、@TARGET_DESCRIPTIONでの状況描写が入る可能性があります
;-------------------------------------------------
IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
	PRINTFORMW 
	RETURN 1
ENDIF

;────────────────────────────────────
;ACT27, 押し倒す
;────────────────────────────────────
;・押し倒す/押し倒し終了
;・既に押し倒している場合、解放になる
;・TFLAG:お仕置きフラグがたっていると発生しない
;・今日の方針が"イチャイチャ"だと発生しない
;・TEQUIP:ペニスバンド装着中には発生しない
;・焦らし中には発生しない
;-------------------------------------------------
@KOJO_ACT_K12_27
;-------------------------------------------------
;TFLAG:ACT派生（=0,押し倒す/=1,押し倒し終了）
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT27_0, 押し倒す			%CALLNAME:MASTER%を押し倒した…
;━━━━━━━━━━━━━━━━━━━━━━━━
;地の文に派生はありませんが、@TARGET_DESCRIPTIONで状況描写が入る可能性があります
;-------------------------------------------------
IF 口上記入チェック(0) && !TFLAG:ACT派生
	PRINTFORMW 
	RETURN 1
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT27_1, 押し倒し終了		%CALLNAME:MASTER%を自由にした…
;━━━━━━━━━━━━━━━━━━━━━━━━
IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
	PRINTFORMW 
	RETURN 1
ENDIF
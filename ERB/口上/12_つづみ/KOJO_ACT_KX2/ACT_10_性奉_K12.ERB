;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	すずきつづみ口上 性交奉仕系アクション口上ファイル
;
;	性交奉仕系アクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;性交奉仕系 ACT95～103
;========================================================================
;・TFLAG:ACT(95=正常位させる/96=後背位させる/97=対面座位させる/98=背面座位させる/99=アナルセックスさせる/103=逆レイプ)
;・TFLAG:ACT分類 == 10に該当
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "性奉")で参照する事が可能
;・原則として調教対象→調教者の挿入となる
;・体位の連携によってTFLAG:ACT派生で派生する
;・各ACTに於ける主導権は@SEX_INITIATIVEにて定義される
;-------------------------------------------------
;※性交中、性交奉仕中からの派生について…
;　一部のACT派生はACT_VARIATIONで定義されつつも、ACT_ABLEで弾かれている現状です
;　ものによっては現在の仕様自体が怪しい気もしますが、この件についての本体側からの回答が得られていません
;　いずれ手が入る可能性もあるので、注釈をつけた上で枠は設けておきます
;-------------------------------------------------
;・戻り値(RESULT)について
;・システム側では口上側のRESULT(終端のRETURN 1など)によって口上が表示されたのか否か、
;・また箇所毎の特有の挙動(媚薬等口上をスキップなど)を制御しています
;・@口上記入チェックの場合代入処理を省略できますが、RESULT値は口上側で決めうちする必要があります
;・特別な理由がない限り、テンプレとしてはRETURN 1を記述しておきます
;-------------------------------------------------

;────────────────────────────────────
;ACT95, 正常位させる
;────────────────────────────────────
;・調教者が調教対象に正常位での挿入を命じる
;・調教対象にペニス、ペニスバンドがない場合は発生しない
;・調教者がペニスバンド装着中の場合は発生しない
;・調教対象が仰向け、かつ性交奉仕中でない場合は発生しない
;・調教対象の勃起度が1000以下の場合は発生しない
;・押し倒し中には発生しない
;・調教者がオトコの場合は発生しない
;・調教対象がオナホール使用中の場合には発生しない
;・性交中からは派生しない
;・背面座位での性交奉仕中からは派生しない
;・Ａ性交奉仕中からは派生しない
;・主導権は調教対象"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;・REACT分類が"暴れる", "拒否"であった場合はキャンセルされる
;・調教対象が童貞なら童貞喪失が起こる
;・調教者が処女なら処女喪失が起こる
;・これらの処理は@ACTION_APPLY2_95にて行われる
;-------------------------------------------------
@KOJO_ACT_K12_95
;-------------------------------------------------
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;※v1.010現在、ACT派生3/4/5はACT_ABLEで規制されているので機能しない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT95, 正常位させる
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT派生によって分岐
;・各派生内で焦らし度、ペニスバンド、イきそう、調教者の下着の有無等で分岐する
;・必要に応じて分岐を増減して下さい
;-------------------------------------------------
IF 口上記入チェック(0)
	;=================================================
	;処女・童貞
	;=================================================
	IF 口上記入チェック(0) && TRUECHECK(TALENT:処女, TALENT:MASTER:童貞) > 0
		;調教者が処女＆調教対象が童貞
		IF TALENT:処女 && TALENT:MASTER:童貞
			PRINTFORMW 
		;調教者が処女
		ELSEIF TALENT:処女
			PRINTFORMW 
		;調教対象が童貞
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:抜かず(調教対象が射精後ペニスを抜かず性交を続行した回数)
	;TFLAG:抜かず > 1などとする事で抜かずの回数参照は可能
	;調教者欲望、技巧、Ｖ/Ａ感覚、性交ABLによって補正を受ける
	;今日の方針が子作りだと+補正
	;要は膣内射精後、抜かずに性交奉仕系ACTが実行されたというシチュエーション
	;性交系ACTの場合TFLAG:抜かずは参照できない
	;=================================================
	;抜かずに続行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:抜かず
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;調教対象がイきそうな場合はTCVAR:MASTER:イきそう
	;調教者の場合はPALAM:快Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;連続(前回ACTが正常位させる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;通常の連続実行
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;TEQUIP:性交奉仕中 == 5(背面座位させる)からは派生しない
	;前回ACTはIS_PREVACTNAME("対面座位させる")等で参照できる
	;ただし、前回の体位はTEQUIPで判定されるので前回ACTでは拾えない可能性がある
	;=================================================
	;体位変更(Ｖ性交奉仕から)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;通常の連続実行
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ性交系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交奉仕から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;基本的に挿入時に調教対象がイきそうであるという状況を想定
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT96,後背位させる
;────────────────────────────────────
;・調教者が調教対象に後背位での挿入を命じる
;・調教対象にペニス、ペニスバンドがない場合は発生しない
;・調教者がペニスバンド装着中の場合は発生しない
;・調教対象が仰向け、かつ性交奉仕中でない場合は発生しない
;・調教対象の勃起度が1000以下の場合は発生しない
;・押し倒し中には発生しない
;・調教者がオトコの場合は発生しない
;・調教対象がオナホール使用中の場合には発生しない
;・性交中からは派生しない
;・対面座位での性交奉仕中からは派生しない
;・Ａ性交奉仕中からは派生しない
;・主導権は調教対象"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;・REACT分類が"暴れる", "拒否"であった場合はキャンセルされる
;・調教対象が童貞なら童貞喪失が起こる
;・調教者が処女なら処女喪失が起こる
;・これらの処理は@ACTION_APPLY2_96にて行われる
;-------------------------------------------------
@KOJO_ACT_K12_96
;-------------------------------------------------
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;※v1.010現在、ACT派生3/4/5はACT_ABLEで規制されているので機能しない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT96,後背位させる
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT派生によって分岐
;・各派生内で焦らし度、ペニスバンド、イきそう、調教者の下着の有無等で分岐する
;・必要に応じて分岐を増減して下さい
;-------------------------------------------------
IF 口上記入チェック(0)
	;=================================================
	;処女・童貞
	;=================================================
	IF 口上記入チェック(0) && TRUECHECK(TALENT:処女, TALENT:MASTER:童貞) > 0
		;調教者が処女＆調教対象が童貞
		IF TALENT:処女 && TALENT:MASTER:童貞
			PRINTFORMW 
		;調教者が処女
		ELSEIF TALENT:処女
			PRINTFORMW 
		;調教対象が童貞
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:抜かず(調教対象が射精後ペニスを抜かず性交を続行した回数)
	;TFLAG:抜かず > 1などとする事で抜かずの回数参照は可能
	;調教者欲望、技巧、Ｖ/Ａ感覚、性交ABLによって補正を受ける
	;今日の方針が子作りだと+補正
	;要は膣内射精後、抜かずに性交奉仕系ACTが実行されたというシチュエーション
	;性交系ACTの場合TFLAG:抜かずは参照できない
	;=================================================
	;抜かずに続行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:抜かず
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;調教対象がイきそうな場合はTCVAR:MASTER:イきそう
	;調教者の場合はPALAM:快Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;連続(前回ACTが後背位させる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;通常の連続実行
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;TEQUIP:性交奉仕中 == 4(対面座位させる)からは派生しない
	;前回ACTはIS_PREVACTNAME("正常位させる")等で参照できる
	;ただし、前回の体位はTEQUIPで判定されるので前回ACTでは拾えない可能性がある
	;=================================================
	;体位変更(Ｖ性交奉仕から)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;通常の連続実行
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ性交系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交奉仕から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;基本的に挿入時に調教対象がイきそうであるという状況を想定
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT97, 対面座位させる
;────────────────────────────────────
;・調教者が調教対象に対面座位での挿入を命じる
;・調教対象にペニス、ペニスバンドがない場合は発生しない
;・調教者がペニスバンド装着中の場合は発生しない
;・調教対象が仰向け、かつ性交奉仕中でない場合は発生しない
;・調教対象の勃起度が1000以下の場合は発生しない
;・押し倒し中には発生しない
;・調教者がオトコの場合は発生しない
;・調教対象がオナホール使用中の場合には発生しない
;・性交中からは派生しない
;・背面座位での性交奉仕中からは派生しない
;・Ａ性交奉仕中からは派生しない
;・主導権は調教対象"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;・REACT分類が"暴れる", "拒否"であった場合はキャンセルされる
;・調教対象が童貞なら童貞喪失が起こる
;・調教者が処女なら処女喪失が起こる
;・これらの処理は@ACTION_APPLY2_97にて行われる
;-------------------------------------------------
@KOJO_ACT_K12_97
;-------------------------------------------------
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;※v1.010現在、ACT派生3/4/5はACT_ABLEで規制されているので機能しない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT97, 対面座位させる
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT派生によって分岐
;・各派生内で焦らし度、ペニスバンド、イきそう、調教者の下着の有無等で分岐する
;・必要に応じて分岐を増減して下さい
;-------------------------------------------------
IF 口上記入チェック(0)
	;=================================================
	;処女・童貞
	;=================================================
	IF 口上記入チェック(0) && TRUECHECK(TALENT:処女, TALENT:MASTER:童貞) > 0
		;調教者が処女＆調教対象が童貞
		IF TALENT:処女 && TALENT:MASTER:童貞
			PRINTFORMW 
		;調教者が処女
		ELSEIF TALENT:処女
			PRINTFORMW 
		;調教対象が童貞
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:抜かず(調教対象が射精後ペニスを抜かず性交を続行した回数)
	;TFLAG:抜かず > 1などとする事で抜かずの回数参照は可能
	;調教者欲望、技巧、Ｖ/Ａ感覚、性交ABLによって補正を受ける
	;今日の方針が子作りだと+補正
	;要は膣内射精後、抜かずに性交奉仕系ACTが実行されたというシチュエーション
	;性交系ACTの場合TFLAG:抜かずは参照できない
	;=================================================
	;抜かずに続行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:抜かず
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;調教対象がイきそうな場合はTCVAR:MASTER:イきそう
	;調教者の場合はPALAM:快Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;連続(対面座位させるから)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;通常の連続実行
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;TEQUIP:性交奉仕中 == 2(後背位させる)/TEQUIP:性交奉仕中 == 5(背面座位させる)からは派生しない
	;前回ACTはIS_PREVACTNAME("正常位させる")等で参照できる
	;ただし、前回の体位はTEQUIPで判定されるので前回ACTでは拾えない可能性がある
	;=================================================
	;体位変更(Ｖ性交奉仕から)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;通常の連続実行
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ性交系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交奉仕から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;基本的に挿入時に調教対象がイきそうであるという状況を想定
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT98,背面座位させる
;────────────────────────────────────
;・調教者が調教対象に背面座位での挿入を命じる
;・調教対象にペニス、ペニスバンドがない場合は発生しない
;・調教者がペニスバンド装着中の場合は発生しない
;・調教対象が仰向け、かつ性交奉仕中でない場合は発生しない
;・調教対象の勃起度が1000以下の場合は発生しない
;・押し倒し中には発生しない
;・調教者がオトコの場合は発生しない
;・調教対象がオナホール使用中の場合には発生しない
;・性交中からは派生しない
;・正常位、対面座位での性交奉仕中からは派生しない
;・Ａ性交奉仕中からは派生しない
;・主導権は調教対象"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;・REACT分類が"暴れる", "拒否"であった場合はキャンセルされる
;・調教対象が童貞なら童貞喪失が起こる
;・調教者が処女なら処女喪失が起こる
;・これらの処理は@ACTION_APPLY2_98にて行われる
;-------------------------------------------------
@KOJO_ACT_K12_98
;-------------------------------------------------
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;※v1.010現在、ACT派生3/4/5はACT_ABLEで規制されているので機能しない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT98,背面座位させる
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT派生によって分岐
;・各派生内で焦らし度、ペニスバンド、イきそう、調教者の下着の有無等で分岐する
;・必要に応じて分岐を増減して下さい
;-------------------------------------------------
IF 口上記入チェック(0)
	;=================================================
	;処女・童貞
	;=================================================
	IF 口上記入チェック(0) && TRUECHECK(TALENT:処女, TALENT:MASTER:童貞) > 0
		;調教者が処女＆調教対象が童貞
		IF TALENT:処女 && TALENT:MASTER:童貞
			PRINTFORMW 
		;調教者が処女
		ELSEIF TALENT:処女
			PRINTFORMW 
		;調教対象が童貞
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:抜かず(調教対象が射精後ペニスを抜かず性交を続行した回数)
	;TFLAG:抜かず > 1などとする事で抜かずの回数参照は可能
	;調教者欲望、技巧、Ｖ/Ａ感覚、性交ABLによって補正を受ける
	;今日の方針が子作りだと+補正
	;要は膣内射精後、抜かずに性交奉仕系ACTが実行されたというシチュエーション
	;性交系ACTの場合TFLAG:抜かずは参照できない
	;=================================================
	;抜かずに続行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:抜かず
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;調教対象がイきそうな場合はTCVAR:MASTER:イきそう
	;調教者の場合はPALAM:快Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;連続(前回ACTが背面座位させる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;通常の連続実行
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;TEQUIP:性交奉仕中 == 1(正常位させる)/TEQUIP:性交奉仕中 == 4(対面座位させる)からは派生しない
	;前回ACTはIS_PREVACTNAME("後背位させる")等で参照できる
	;ただし、前回の体位はTEQUIPで判定されるので前回ACTでは拾えない可能性がある
	;=================================================
	;体位変更(Ｖ性交奉仕から)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;通常の連続実行
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ性交系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交奉仕から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;基本的に挿入時に調教対象がイきそうであるという状況を想定
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT99, アナルセックスさせる
;────────────────────────────────────
;・調教者が調教対象にアヌスへの挿入を命じる
;・調教対象にペニス、ペニスバンドがない場合は発生しない
;・調教者がペニスバンド装着中の場合は発生しない
;・調教対象が仰向け、かつ性交奉仕中でない場合は発生しない
;・調教対象の勃起度が1000以下の場合は発生しない
;・ターン方針が"ソフト"でＡ性交奉仕中でない場合は発生しない
;・押し倒し中には発生しない
;・調教者がオトコの場合は発生しない
;・調教対象がオナホール使用中の場合には発生しない
;・性交中からは派生しない
;・Ｖ性交奉仕中からは派生しない
;・調教者のＡ感覚が2以下の場合は発生しない
;・主導権は調教対象"SEX_INITIATIVE(MASTER)"
;-------------------------------------------------
;・REACT分類が"暴れる", "拒否"であった場合はキャンセルされる
;-------------------------------------------------
@KOJO_ACT_K12_99
;-------------------------------------------------
;・TFLAG:ACT派生(0=通常/1=連続/2=Ａ性交系から/3=Ｖ性交奉仕系から/4=Ｖ性交系から)
;※v1.010現在、ACT派生2から4はACT_ABLEで規制されているので機能していない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT99, アナルセックスさせる
;━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT派生1(連続)、ACT派生2 TO 5(Ａ性交/Ｖ性交奉仕/Ｖ性交)、その他で分岐
;・各派生内でペニスバンドの有無で分岐する
;・必要に応じて分岐を増減して下さい
;-------------------------------------------------
IF 口上記入チェック(0)
	;TFLAG:抜かず(調教対象が射精後ペニスを抜かず性交を続行した回数)
	;TFLAG:抜かず > 1などとする事で抜かずの回数参照は可能
	;調教者欲望、技巧、Ｖ/Ａ感覚、性交ABLによって補正を受ける
	;今日の方針が子作りだと+補正
	;要は膣内射精後、抜かずに性交奉仕系ACTが実行されたというシチュエーション
	;性交系ACTの場合TFLAG:抜かずは参照できない
	;=================================================
	;抜かずに続行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:抜かず
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;調教対象がイきそうな場合はTCVAR:MASTER:イきそう
	;調教者の場合はPALAM:快Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;連続(前回ACTがアナルセックスさせる)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;通常の連続実行
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ性交奉仕系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ性交系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;基本的に挿入時に調教対象がイきそうであるという状況を想定
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 口上記入チェック(0)
		PRINTFORMW 
		RETURN 1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT103, 逆レイプ
;────────────────────────────────────
;・調教者が騎乗位で強引に性交渉に及ぶ
;・調教対象にペニス、ペニスバンドがない場合は発生しない
;・調教者がペニスバンド装着中の場合は発生しない
;・調教対象の勃起度が1000以下の場合は発生しない
;・調教者がオトコの場合は発生しない
;・調教対象がオナホール使用中の場合には発生しない
;・性交中からは派生しない
;・Ａ性交奉仕中からは派生しない
;・主導権は調教対象"SEX_INITIATIVE(TARGET)"
;-------------------------------------------------
;・他の性交奉仕系と異なり、調教対象は抵抗できない
;・調教対象が童貞なら童貞喪失が起こる
;・調教者が処女なら処女喪失が起こる
;・これらの処理は@ACTION_APPLY2_103にて行われる
;-------------------------------------------------
@KOJO_ACT_K12_103
;-------------------------------------------------
;・TFLAG:ACT派生(0=通常/1=連続/2=Ｖ性交奉仕系から/3=Ａ性交系から/4=Ｖ性交系から/5=Ａ性交奉仕系から)
;・v1.010現在、ACT派生3/4/5はACT_ABLEで規制されているので機能しない
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━━
;ACT103, 逆レイプ
;━━━━━━━━━━━━━━━━━━━━━━━━━
;・地の文はACT派生によって分岐
;・各派生内で焦らし度、ペニスバンド、イきそう、調教者の下着の有無等で分岐する
;・必要に応じて分岐を増減して下さい
;-------------------------------------------------
IF 口上記入チェック(100)
	;=================================================
	;処女・童貞
	;=================================================
	IF 口上記入チェック(100) && TRUECHECK(TALENT:処女, TALENT:MASTER:童貞) > 0
		;調教者が処女＆調教対象が童貞
		IF TALENT:処女 && TALENT:MASTER:童貞
			PRINTFORMW 
		;調教者が処女
		ELSEIF TALENT:処女
			PRINTFORMW 
		;調教対象が童貞
		ELSE
			CALL PRINTFORMW2T , "「さて、あなたは初めてみたいだけど」"
			CALL PRINTFORMW2T , "「今からセックスを行うわ」"
			CALL PRINTFORMW2T , "「性行為。おちんちんをおまんこに入れる行い」"
			CALL PRINTFORMW2T , "「中出ししても大丈夫。精液は、私の中で保存されるから」"
		ENDIF
		RETURN 1
	ENDIF
	
	;TFLAG:抜かず(調教対象が射精後ペニスを抜かず性交を続行した回数)
	;TFLAG:抜かず > 1などとする事で抜かずの回数参照は可能
	;調教者欲望、技巧、Ｖ/Ａ感覚、性交ABLによって補正を受ける
	;今日の方針が子作りだと+補正
	;要は膣内射精後、抜かずに性交奉仕系ACTが実行されたというシチュエーション
	;性交系ACTの場合TFLAG:抜かずは参照できない
	;=================================================
	;抜かずに続行
	;=================================================
	IF 口上記入チェック(0) && TFLAG:抜かず
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;---------------------------------------------
	;調教対象がイきそうな場合はTCVAR:MASTER:イきそう
	;調教者の場合はPALAM:快Ｖ > 8500
	;とか、そんな感じで参照して下さい
	;---------------------------------------------
	
	;=================================================
	;連続(前回ACTが逆レイプ)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 1
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;通常の連続実行
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;どの体位からも派生する可能性あり
	;=================================================
	;体位変更(Ｖ性交奉仕から)
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 2
		;焦らし度による派生
		SELECTCASE TCVAR:MASTER:焦らし度
			;焦らし中
			CASE IS > 1
				PRINTFORMW 
			;焦らし終了
			CASE 1
				PRINTFORMW 
			;焦らしなし
			CASEELSE
				PRINTFORMW 
		ENDSELECT
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 3
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ｖ性交系から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 4
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;v1.010現在、ACT_ABLEで弾かれている
	;=================================================
	;Ａ性交奉仕から
	;=================================================
	IF 口上記入チェック(0) && TFLAG:ACT派生 == 5
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;基本的に挿入時に調教対象がイきそうであるという状況を想定
	;=================================================
	;調教対象がイきそう
	;=================================================
	IF 口上記入チェック(0) && TCVAR:MASTER:イきそう
		PRINTFORMW 
		RETURN 1
	ENDIF
	
	;=================================================
	;その他・条件無し
	;=================================================
	IF 口上記入チェック(100)
SELECTCASE RAND:3
		CASE 0
		CALL PRINTFORMW2T , "「あなたの精液を、おまんこで搾り取ってあげる」"
		CALL PRINTFORMW2T , "「何も考えなくていい、ただ、あなたは身を任せるだけ」"
		CASE 1
		CALL PRINTFORMW2T , "「あなたのおちんちんは今、私のおまんこの中」"
		CALL PRINTFORMW2T , "「ぬるぬるして、ぐちょぐちょになって」"
		CALL PRINTFORMW2T , "「身体が熱くなって、おちんちんのことしか考えられなくなる」"
		CASE 2
		CALL PRINTFORMW2T , "「あなたの悦ぶ腰の振り方が、なんとなくわかってきたわ」"
		PRINTFORMDW そういうとつづみは腰を艷かしくグラインドさせ、膣をぐにゅぐにゅとうねらせる
		ENDSELECT
		RETURN 1
	ENDIF
ENDIF
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	[ここに口上主名を入れる]口上 奉仕系アクション口上ファイル
;
;	奉仕系アクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;奉仕系 ACT50～57
;========================================================================
;・TFLAG:ACT(50=手で愛撫を強制/51=口で愛撫を強制/52=パイズリ/53=素股/55=足舐め強制/56=イラマチオ/57=胸愛撫強制)
;・TFLAG:ACT分類 == 5に該当
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "奉仕")で拾う事が可能
;・調教対象に、調教者の身体を愛撫するように命令する
;・基本的には命令なので、この段階では従うかどうか確定していない(ACT56,イラマチオは拒否できない)
;-------------------------------------------------
;・ACT54,足コキは12/04/08版で粛清
;-------------------------------------------------

;────────────────────────────────────
;ACT50, 手で愛撫を強制
;────────────────────────────────────
;・愛撫強制/手淫強制/ペニスバンド手淫強制
;・調教対象(MASTER)に、手による愛撫を強制する
;・調教者にペニスがあると必ず手淫強制になる(ペニスがあると通常の愛撫強制は起こらない)
;・調教者がペニスバンド装着中はペニスバンド手淫強制になる
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"ハード", "異常"だと発生しない
;・押し倒し中には発生しない
;・TEQUIP(縄/三角木馬)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・調教対象が従うか否かはこの時点では未定
;・REACT分類が拒否/暴れる/逃げるの場合は拒絶扱いとなる
;-------------------------------------------------
@KOJO_ACT_KX1_50
;-------------------------------------------------
;・TFLAG:ACT派生(0=愛撫強制/1=手淫強制/2=ペニスバンド手淫強制)
;-------------------------------------------------
;・地の文はACT派生毎に分岐。各ACT派生内では分岐なし
;・ACT派生0,愛撫の時のみ快Ｂ/快ＡSOURCE増加
;・その他の場合は快Ｃ限定
;-------------------------------------------------

;この構造で初回分岐に落ちたら派生に関わらず一度きりの表示
;派生毎に初回分岐を設けたいなら各ACT派生内で処理する事をお勧めします
;FIRSTACT(0)/FIRSTACT(1)/FIRSTACT(2)のように分けてやれば派生毎に初回を参照できます
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50, 初回
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;ACT派生1, 手淫強制
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT派生 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;ACT派生2, ペニスバンド手淫強制
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT派生 == 2
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;上層の分岐を埋めた場合はTFLAG:ACT派生 == 0と同義となる
	;ここだけで済ませるなら、汎用的に使えるよう留意すべき箇所
	;=================================================
	;その他・条件無し
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_0, 愛撫強制「自分の性器を手で愛撫しろと命令する」
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT派生
	;=================================================
	;調教者がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:イきそう
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_1, 手淫強制「自分の男性器を手で扱けと命令する」
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 1
	;=================================================
	;調教者がイキそう
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:イきそう
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;ペニスバンドへの愛撫は快ＣSOURCEが減少し、快ＶSOURCEが増加する
;装着している時点で対象が処女である可能性はない
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT50_2, ペニスバンド手淫強制「ペニスバンドを手で掴んで動かせと命令する」
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 2
	;=================================================
	;調教者がイキそう
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:イきそう
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT51, 口で愛撫を強制
;────────────────────────────────────
;・クンニ/フェラチオ/顔面騎乗/顔面騎乗アナル/
;・調教対象に、口による愛撫を強制する
;・調教者がふたなりの場合、調教対象が仰向けなら1/2で顔面騎乗、そうでなければフェラ強制になる
;・調教者にペニスがなく、調教対象が仰向けなら顔面騎乗になる
;・顔面騎乗が選ばれていたとき、顔面騎乗アナルに変化することがある
;・全ての条件から漏れた場合は通常のクンニとなる
;・TFLAG:お仕置きフラグが立っていると発生しない
;・押し倒し中には発生しない
;・TEQUIP(ボールギャグ/三角木馬)装着中は発生しない
;・調教者がペニスバンドを装着している場合は発生しない
;・性交中/性交奉仕中には発生しない
;・REACT分類が拒否/暴れる/逃げるの場合は拒絶扱いになる
;-------------------------------------------------
@KOJO_ACT_KX1_51
;-------------------------------------------------
;・TFLAG:ACT派生(0=クンニ/1=フェラ/2=顔面騎乗/3=顔面騎乗アナル)
;-------------------------------------------------


;とりあえずFIRSTACT(0)で最上層に初回を配置しましたが、各ACT派生内で処理するのも良いでしょう
;FIRSTACTのイベント番号を被らないようにすれば、派生毎に初回判定ができます
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51, 初回
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;ACT派生がフェラ強制
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;ACT派生が顔面騎乗
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT派生 == 2
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;ACT派生が顔面騎乗アナル
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT派生 == 3
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;上層の分岐を全て埋めた場合はクンニ強制扱いになる
	;派生毎に書き分けないのであれば、この箇所を汎用的に使える内容にした上で不要な分岐を削除すると良いでしょう
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;調教対象のＣを責めつつ顔面騎乗になるシチュエーション
;顔面騎乗になる場合は調教者のペニス、ペニスバンド等を考慮する必要なし
;必要に応じて
;IF TEQUIP:ペニスバンド
;ELSEIF PENIS(MASTER)
;ELSE
;等の分岐を設けて使用して下さい
;TEQUIP:調教対象Ｃ使用 == 1は手で愛撫中
;TEQUIP:調教対象Ｃ使用 == 2は口で愛撫中となります
;一応口と手それぞれの分岐を設けましたが
;調教者の素質によっては口で愛撫が殆ど出なかったりするので、そのあたりを見極めた上で分岐を増減して下さい
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象がＣを責められている
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && GROUPMATCH(TEQUIP:調教対象Ｃ使用, 1, 2) > 0
	;=================================================
	;膣内射精後3ターン以内		膣口からは先程たっぷりと搾り取った精液がどろりとあふれでている…
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && INRANGE(TCVAR:MASTER:膣内射精後経過ターン, 1, 2)
		;口で愛撫中
		IF TEQUIP:調教対象Ｃ使用 == 2
			PRINTFORMW 
		;手で愛撫中
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;顔面騎乗アナル派生			目の前にひくつく菊門がさらけ出されている……
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT派生 == 3
		;口で愛撫中
		IF TEQUIP:調教対象Ｃ使用 == 2
			PRINTFORMW 
		;手で愛撫中
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;通常の顔面騎乗を想定。@VAGINA_DESCRIPTIONは描写系関数
	;=================================================
	;その他・条件なし			無防備な%VAGINA_DESCRIPTION(TARGET)%がさらけ出されている……
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;口で愛撫中
		IF TEQUIP:調教対象Ｃ使用 == 2
			PRINTFORMW 
		;手で愛撫中
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_0, クンニ強制
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT派生
	;=================================================
	;調教者がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:イきそう
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;連続実行
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:ACT == TFLAG:前ACT
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_1, フェラチオ
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 1
	;=================================================
	;調教者がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:イきそう
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;連続実行
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:ACT == TFLAG:前ACT
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_2, 顔面騎乗
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 2
	;=================================================
	;調教者がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:イきそう
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;連続実行
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT == TFLAG:前ACT
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;膣内射精後3ターン以内
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && INRANGE(TCVAR:MASTER:膣内射精後経過ターン, 1, 2)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT51_3, 顔面騎乗アナル
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 3
	;快Ｃ以外はTCVAR:イきそうでは拾えないのでPALAMで
	;=================================================
	;調教者の快Ａが一定以上「イきそう」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PALAM:快Ａ >= 8500
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;連続実行
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT == TFLAG:前ACT
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT52, パイズリ
;────────────────────────────────────
;・調教対象にパイズリでの奉仕を強制する
;・調教対象がオトコの場合は発生しない
;・調教者にペニスがない場合は発生しない
;・ターン方針が"異常"の場合は発生しない
;・押し倒し中には発生しない
;・TEQUIP(縄/三角木馬)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・REACT分類が拒否/暴れる/逃げるの場合は拒絶扱いとなる
;-------------------------------------------------
@KOJO_ACT_KX1_52
;-------------------------------------------------
;地の文に分岐なし。@TARGET_DESCRIPTIONによる状況描写が入る可能性あり
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT52, パイズリ
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT派生
	;=================================================
	;初回
	;記入チェック
	LOCAL = 0
	;=================================================
	IF LOCAL && FIRSTACT(0)
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教者がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:イきそう
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;調教対象のＢ絶頂が近いというシチュエーション
	;=================================================
	;調教対象の快Ｂが一定以上
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PALAM:MASTER:快Ｂ >= 8500
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教対象が巨乳
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:巨乳
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教対象が貧乳
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:貧乳
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT53, 素股
;────────────────────────────────────
;・調教対象にペニスバンド、もしくはペニスを股に挟んで擦れと命令する
;・調教者に棒がない(ペニス/ペニスバンド)場合は発生しない
;・調教対象にペニスがある場合は発生しない
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"異常"の場合は発生しない
;・押し倒し中には発生しない
;・TEQUIP:三角木馬装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・調教対象に足がないと発生しない(現状では屠自古が調教対象である場合のみと考えられる)
;・REACT分類が拒否/暴れる/逃げるである場合は拒絶扱い
;-------------------------------------------------
@KOJO_ACT_KX1_53
;-------------------------------------------------
;・TFLAG:ACT派生(0=素股/1=ペニスバンド素股)
;-------------------------------------------------
;・顔面騎乗系、自慰系は解除される
;・地の文はACT派生で分岐。派生内では分岐なし。@TARGET_DESCRIPTIONによる状況描写が入る可能性あり
;-------------------------------------------------

;取り敢えず最上層に配置しましたが、ACT派生内で処理してもいいと思います
;FIRSTACTの引数を揃えるか、FIRSTACT(0), FIRSTACT(1)として個別に初回判定を拾うかは書き手次第
;━━━━━━━━━━━━━━━━━━━━━━━━
;初回
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;ペニスバンド素股強制
	;記入チェック
	LOCAL = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;TFLAG:ACT派生 == 1の分岐を書いた場合はACT派生 == 0(素股強制)扱いとなる
	;上層の分岐を放置してここだけにするなら、汎用的に使えるよう留意すべき箇所
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;調教者にペニスがある場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_0, 素股強制
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT派生
	;=================================================
	;イキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TRUECHECK(TCVAR:TARGET:イきそう, TCVAR:MASTER:イきそう) > 0
		;調教者(TARGET)がイきそう
		IF TCVAR:TARGET:イきそう
			PRINTFORMW 
		;調教対象(MASTER)がイきそう
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;潤滑十分
		IF PALAM:MASTER:潤滑 >= 500
			PRINTFORMW 
		;潤滑不足
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;調教者がペニスバンド装着中の場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT53_1, ペニスバンド素股強制
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 1
	;=================================================
	;イキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TRUECHECK(TCVAR:TARGET:イきそう, TCVAR:MASTER:イきそう) > 0
		;調教者(TARGET)がイきそう
		IF TCVAR:TARGET:イきそう
			PRINTFORMW 
		;調教対象(MASTER)がイきそう
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;潤滑十分
		IF PALAM:MASTER:潤滑 >= 500
			PRINTFORMW 
		;潤滑不足
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT55, 足舐め強制
;────────────────────────────────────
;・調教者が足を舐めろと命令してくる
;・異常系から発生する場合あり
;・ターン方針が"ソフト"だと発生しない
;・本日の方針が"イチャイチャ"だと発生しない
;・押し倒し中には発生しない
;・TEQUIP(ボールギャグ/三角木馬)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・調教者に足がない場合は発生しない
;・REACT分類が拒否/暴れる/逃げるの場合は拒絶扱い
;-------------------------------------------------
@KOJO_ACT_KX1_55
;-------------------------------------------------
;・地の文に分岐なし。@TARGET_DESCRIPTIONで状況描写が入る可能性あり
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT55, 足舐め強制
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT派生
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;前回COMが許しを乞った
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("許しを乞う")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;お仕置きモード
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:お仕置きフラグ
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教対象が仰向け
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:仰向け
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教者が靴下を履いている
	;(デフォルトで靴下履いていないキャラは表示されない)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:靴下
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT56, イラマチオ
;────────────────────────────────────
;・命令するのではなく、強引にペニスを口に突っ込む
;・調教対象の反応は未定だが、拒絶する事はできない
;・加虐系から発生する場合あり
;・ターン方針が"ノーマル", "ソフト"だと発生しない
;・押し倒し中には発生しない
;・TEQUIP(ボールギャグ/三角木馬)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・調教者にペニスがない場合は発生しない(ペニスバンドによるイラマチオは無い)
;-------------------------------------------------
@KOJO_ACT_KX1_56
;-------------------------------------------------
;・自慰中であれば解除される
;・地の文に分岐なし。地の文は↓
;・%CALLNAME:TARGET%は%CALLNAME:MASTER%の頭をつかんで、乱暴に%CALLNAME:MASTER%の喉まで突き入れた…
;-------------------------------------------------
;・調教者の勃起度合いはTCVAR:勃起で参照できる
;・TCVAR:勃起(0=まだ柔らかい/1=徐々に硬度を増しつつある/2=勃起した/2以上=反り返った)的な感じで
;-------------------------------------------------

;必要に応じて分岐を増減して下さい
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT56, イラマチオ
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT派生
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教者がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:TARGET:イきそう
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教対象が仰向け
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:仰向け
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT57, 胸愛撫強制
;────────────────────────────────────
;・調教対象に、調教者の胸を愛撫するよう強制する
;・調教者がオトコの場合は発生しない
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"異常"だと発生しない
;・押し倒し中には発生しない
;・TEQUIP:縄装着中は口で胸愛撫強制に派生する。ただしボールギャグを装着中の場合は弾かれる
;・ボールギャグがなければ、1/2の確率で口で愛撫になる
;・REACT分類が拒否/暴れる/逃げるの場合は拒絶扱い
;・PALAMが恥情先行か反抗先行かで"恥ずかしくて奉仕できなかった/嫌がって要求に応じなかった"に派生する
;-------------------------------------------------
@KOJO_ACT_KX1_57
;-------------------------------------------------
;・TFLAG:ACT派生(0=手で胸愛撫強制/2=口で胸愛撫強制)
;-------------------------------------------------
;・地の文はACT派生で分岐。派生内では分岐なし
;・@TARGET_DESCRIPTIONによる状況描写が入る可能性あり
;・顔面騎乗は解除される
;・REACTにおいて、仰向けでなく、ACT派生が口で愛撫でない場合、1/2で後ろから愛撫に派生する
;-------------------------------------------------

;取り敢えず最上層に配置しましたが、ACT派生内で処理してもいいと思います
;FIRSTACTの引数を揃えるか、FIRSTACT(0), FIRSTACT(1)として個別に初回判定を拾うかは書き手次第
;━━━━━━━━━━━━━━━━━━━━━━━━
;初回
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;ACT派生2, 口で胸愛撫を強制
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:ACT派生 == 2
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;TFLAG:ACT派生 == 2分岐を埋めた場合は!TFLAG:ACT派生分岐として機能
	;ここだけで済ませるのであれば、どちらでも使える内容を
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_0, 手で胸愛撫を強制
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT派生
	;以前は体位毎によっては規制されていたが、現在では全ての体位で発生する
	;場合によっては無茶な体制になるが、これは現状では仕様ということで
	;=================================================
	;調教者挿入中
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:性交中
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教対象挿入中
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:性交奉仕中
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教者がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PALAM:快Ｂ > 8500
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT57_2, 口で胸愛撫を強制
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 2
	;以前は体位毎によっては規制されていたが、現在では全ての体位で発生する
	;場合によっては無茶な体制になるが、これは現状では仕様ということで
	;=================================================
	;調教者挿入中
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:性交中
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教対象挿入中
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:性交奉仕中
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教者がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PALAM:快Ｂ > 8500
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	[ここに口上主名を入れる]口上 羞恥系アクション口上ファイル
;
;	羞恥系アクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;羞恥系 ACT40～45
;========================================================================
;・TFLAG:ACT(40=自慰/41=秘貝開帳/42=自慰みせつけ/43=羞恥プレイ/44=野外プレイ/45=放尿/46=コスプレ(未実装))
;・TFLAG:ACT分類 == 4に該当
;・汎用リアクションにおいてはMENUMATCH(TFLAG:ACT, "羞恥")で拾う事が可能
;・ACTによっては拒否が可能であったりなかったり
;-------------------------------------------------

;────────────────────────────────────
;ACT40, 自慰
;────────────────────────────────────
;・オナニーしてみせろと命令する
;・この段階で命令に従うか否かは未定
;・REACT分類が消極的従う/平然と従う/積極的従う/受け入れる以外の場合は、全てREACT分類"拒否"扱い
;・ターン方針が"異常"だと発生しない
;・押し倒し中である場合は発生しない
;・すでに自慰中である場合は発生しない
;・TEQUIP(縄/三角木馬)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・このACTに派生はない
;-------------------------------------------------
@KOJO_ACT_KX1_40
;-------------------------------------------------
;・地の文に分岐はなし
;・@TARGET_DESCRIPTIONで状況描写が入る場合あり
;・調教対象のペニスの有無、ペニス無しの場合はペニスバンドの有無による書き分けが考えられる
;・調教対象がオトコであれば快Ｂは発生しない
;-------------------------------------------------

;テンプレとしては状態毎に初回分岐を分けるのもどうかと思ったので統合して最上位に配置
;派生毎に初回判定したい場合は個別の分岐に切り分け、FIRSTACT(0)/FIRSTACT(1)/FIRSTACT(2のようにすれば良い)
;調教対象にペニスが無い場合、ペニスバンド分岐とその他分岐の両方に落ちる可能性があるので
;FIRSTACTのイベント番号を揃えてしまうか、敢えて双方を生かすかは書き手次第
;━━━━━━━━━━━━━━━━━━━━━━━━
;初回
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;調教対象にペニス有り		TALENT:MASTER:オトコ/TALENT:MASTER:ふたなり等で別けられない事もない
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && PENIS(MASTER)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	;=================================================
	;ペニスバンド装着中			当然の事ながら、ペニスは無い
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:ペニスバンド
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;上の分岐を全て埋めれば女MASTER分岐に相当
	;放置してこの箇所だけにするなら汎用的に記述することを推奨
	;=================================================
	;その他						女MASTERでペニスバンドを装着していない場合
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;MASTERがオトコ、もしくはふたなりである場合
;その他・条件なしを書いた上で、必要に応じて分岐を増減して下さい
;箇所によってはTALENT:MASTER:オトコなりTALENT:MASTER:ふたなりで別けても良いと思います
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象にペニスがある場合
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && PENIS(MASTER)
	;=================================================
	;前COMがイかせて！
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("イかせて！")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;前COMが気持ち良くして
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("気持ち良くして")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教対象がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:MASTER:イきそう
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教者も自慰中
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:自慰中, 1)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;オナホール装着中
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:オナホール
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;アナルビーズ /アナルバイブ装着中
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TRUECHECK(TEQUIP:アナルビーズ, TEQUIP:アナルバイブ) > 0
		;アナルビーズ使用中
		IF TEQUIP:アナルビーズ
			PRINTFORMW 
		;アナルバイブ使用中
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;満足ボーナスで"ハード"を選んでいる場合はお仕置きフラグが立ったままになるので
	;LOCAL:1 = PERCENT(X)で確率にした方が良い場合もある
	;=================================================
	;お仕置きモード
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:お仕置きフラグ
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;必要に応じてこのネストを開放し、分岐を加えた上で使用して下さい
;!PENIS(MASTER)分岐に統合してしまう手もあります
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象がペニスバンドを装着している場合
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TEQUIP:ペニスバンド
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;必要に応じて分岐を増減して下さい
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象にペニスがない場合
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !PENIS(MASTER)
	;=================================================
	;前COMがイかせて！
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("イかせて！")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;前COMが気持ち良くして
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_PREVCOMNAME("気持ち良くして")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教対象がイキそう
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TCVAR:MASTER:イきそう
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教者も自慰中
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:自慰中, 1)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;バイブ/アナルバイブ装着中
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TRUECHECK(TEQUIP:バイブ, TEQUIP:アナルバイブ) > 0
		;バイブ使用中
		IF TEQUIP:バイブ
			PRINTFORMW 
		;アナルバイブ使用中
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;満足ボーナスで"ハード"を選んでいる場合はお仕置きフラグが立ったままになるので
	;LOCAL:1 = PERCENT(X)で確率にした方が良い場合もある
	;=================================================
	;お仕置きモード
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:お仕置きフラグ
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT41, 秘貝開帳
;────────────────────────────────────
;・調教者が調教対象の秘貝を開帳する。所謂「くぱぁ」
;・調教対象がオトコの場合は発生しない
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"異常"の場合は発生しない
;・TEQUIP:三角木馬装着中は発生しない
;・押し倒し中は発生しない
;・性交中/性交奉仕中は発生しない
;・このACTに派生はない
;・現在リアクション後の@ACTION_APPLY2に処理なし
;-------------------------------------------------
@KOJO_ACT_KX1_41
;-------------------------------------------------
;・TEQUIP(顔面騎乗/自慰中)は解除される
;・下半身全部強制脱衣の処理あり
;-------------------------------------------------

;TEQUIP(アイマスク/縄)等の分岐を設けてやるのも面白いかも知れません
;必要に応じて分岐を増減して下さい
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT41, 秘貝開帳
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		;調教対象が処女
		IF TALENT:MASTER:処女
			PRINTFORMW 
		;その他
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教対象が処女
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:処女
		;潤滑十分
		IF PALAM:MASTER:潤滑 >= 500
			PRINTFORMW 
		;潤滑不足
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		;潤滑十分
		IF PALAM:MASTER:潤滑 >= 500
			PRINTFORMW 
		;潤滑不足
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT42, 自慰見せつけ
;────────────────────────────────────
;・調教者が見せ付けるように自慰を始める
;・TFLAG:お仕置きフラグが立っていると発生しない
;・押し倒し中には発生しない
;・調教者がすでに自慰中である場合は発生しない
;・性交中/性交奉仕中には発生しない
;-------------------------------------------------
@KOJO_ACT_KX1_42
;-------------------------------------------------
;調教者欲望、技巧、調教レベル、調教者技巧、器用な指先素質に応じて快Ｃ/快ＢSOURCE増加
;バイブ所持の場合の処理は0616版時点では封印されている
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者にペニスがある場合
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && PENIS(TARGET)
	;=================================================
	;調教対象も自慰中
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:自慰中, 0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ペニスバンド装着中
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TEQUIP:調教者ペニスバンド
	;=================================================
	;調教対象も自慰中
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:自慰中, 0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教者にペニスがない場合
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !PENIS(TARGET)
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;アイマスク装着中
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:アイマスク
		;調教対象も自慰中
		IF GETBIT(TEQUIP:自慰中, 0)
			PRINTFORMW 
		;その他
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教対象も自慰中
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GETBIT(TEQUIP:自慰中, 0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;拘束中
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:縄
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT43, 羞恥プレイ
;────────────────────────────────────
;・調教対象を大鏡の前に連れて行く
;・TFLAG:お仕置きフラグが立っていると発生しない
;・ターン方針が"異常"だと発生しない
;・押し倒し中には発生しない
;・TEQUIP:三角木馬装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・既に羞恥プレイ中なら、羞恥プレイ終了になる
;-------------------------------------------------
@KOJO_ACT_KX1_43
;-------------------------------------------------
;・TFLAG:ACT派生(0=開始/1=終了)
;-------------------------------------------------
;・現状本体側では露出SOURCE増加の処理があるのみ
;-------------------------------------------------

;要するに開始、終了時にのみ表示される箇所。地の文も素っ気無い
;簡潔に済ませてしまうか、敢えて凝った事をやるかは書き手次第
;TCVAR:MASTER:状態変化等で分けるのも面白いかも知れません
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT43_0, 羞恥プレイ開始
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT派生
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;条件としてはレアだけど現状、この可能性は一応考慮しておくに越した事はないかなという
	;調教道具が複数ある場合、一度の調教に使用できる道具の数を規制しようか？という話もあるので
	;将来的には微妙な立ち位置の分岐かも知れない
	;=================================================
	;アイマスク装着中
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TEQUIP:アイマスク
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT43_1, 羞恥プレイ終了
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 1
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT44, 野外プレイ
;────────────────────────────────────
;・調教対象に首輪をつけて外に連れ出す
;・ターン方針が"ソフト", "ノーマル"だと発生しない
;・押し倒し中には発生しない
;・TEQUIP(縄/三角木馬)装着中には発生しない
;・性交中/性交奉仕中には発生しない
;・既に野外プレイ中なら、野外プレイ終了になる
;-------------------------------------------------
@KOJO_ACT_KX1_44
;-------------------------------------------------
;・TFLAG:ACT派生(0=開始/1=終了)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT44_0, 野外プレイ開始
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !TFLAG:ACT派生
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTACT(0)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;ここを最初に書きましょう
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT44_1, 野外プレイ終了
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 1
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;────────────────────────────────────
;ACT45, 放尿
;────────────────────────────────────
;・おしっこしてみせろと命令する
;・この段階では命令に従うかどうか確定していない
;・REACT分類が消極的従う/平然と従う/積極的従う/受け入れる以外の場合は、全てREACT分類"拒否"扱い
;・ターン方針が"ソフト"だと発生しない
;・TEQUIP(三角木馬/ペニスバンド)装着中には発生しない
;・押し倒し中には発生しない
;・性交中/性交奉仕中には発生しない
;・尿意(BASE:MASTER:尿意)が5000以下だと発生しない
;-------------------------------------------------
@KOJO_ACT_KX1_45
;-------------------------------------------------
;・ACT派生なし
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;初回
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && FIRSTACT(0)
	;=================================================
	;調教対象がオトコの場合
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:オトコ
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;調教対象がふたなりの場合
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TALENT:MASTER:ふたなり
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;ここで汎用的に記述するか上層の分岐で個別に書き分けるかは書き手次第
	;全て埋めるなら、調教対象が女である場合となります
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象がオトコの場合
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TALENT:MASTER:オトコ
	;=================================================
	;お仕置きモード
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:お仕置きフラグ
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象がふたなりの場合
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TALENT:MASTER:ふたなり
	;=================================================
	;お仕置きモード
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:お仕置きフラグ
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;調教対象が女の子の場合
;記入チェック（=0, 非表示、1, 表示、2, 表示して媚薬等口上をスキップ）
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && !PENIS(MASTER)
	;=================================================
	;お仕置きモード
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:お仕置きフラグ
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他・条件なし
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#
;
;	[ここに口上主名を入れる]口上 会話系個別リアクション口上ファイル
;
;	会話系リアクション口上をまとめたファイル
;
;_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/_/#

;========================================================================
;会話系 REACT0～9
;========================================================================
;・1231版よりTFLAG:REACT負荷が導入され、選択したCOMが自然な行動かどうかが参照できるように
;・0127版以降のリアクション後の地の文は、基本的にREACT負荷で分岐するようになっています
;・TFLAG:REACT負荷(0=自然な行動/1=無理のない行動/2=無理をした行動)
;-------------------------------------------------
;　会話系はあまり汎用処理には向かないので、汎用リアクション(COM)に流さず個別リアクション(REACT)
;　で完結させてしまうのが望ましいと考えます
;-------------------------------------------------

;────────────────────────────────────
;REACT0, 今の気分を聞く・個別リアクション
;────────────────────────────────────
;・ACT派生への代入は調教対象のリアクションに応じて@ACTION_APPLY2_0にて行われる
;-------------------------------------------------
@KOJO_REACT_KX1_0
;-------------------------------------------------
;・TFLAG:ACT派生(0=歯切れ悪く答えた/1=勇気を出して強く当たった/2=皮肉/3=はっきりと今の気持ちを告げた
;・4=まあまあ悪くはない/5=気持ち良いことしたい)
;-------------------------------------------------

;REACT分類が消極的従うで、リアクションが皮肉を言う以外の場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,0 歯切れ悪く質問に答えた
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 0
	;自然な行動
	;=================================================
	;REACT負荷0, 質問にはっきり答えることができなかった
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 歯切れ悪く質問に答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 出掛かった言葉を飲み込み答えを濁した
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;リアクションが強気に応答するの場合
;REACT分類は"拒否"にセットされる
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,1 勇気を出して強く当たった
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 1
	;自然な行動
	;=================================================
	;REACT負荷0, 厳しい言葉を浴びせた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 勇気を出して強く当たった
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 目を逸らし口では強気に振る舞った
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;リアクションが皮肉を言うの場合
;REACT分類は"拒否"にセットされる
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,2 皮肉「今の気分は、と聞かれても」
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 2
	;自然な行動
	;=================================================
	;REACT負荷0, 余裕の笑みで肩をすくめ「今の気分は、と聞かれても」と答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 少し考えてから「今の気分は、と聞かれても」と答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, かろうじて皮肉交じりに答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT分類が積極的従うの場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,3 はっきりと今の気持ちを告げた
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 3
	;自然な行動
	;=================================================
	;REACT負荷0, 一緒に居られることが嬉しいと素直に口にした
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, はっきりと今の気持ちを告げた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 機嫌を損ねないよう、取り繕って答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT分類が平然と従うの場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,4 まあまあ悪くはない
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 4
	;自然な行動
	;=================================================
	;REACT負荷0, そう悪く思ってはいないと答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「まあまあ悪くはない」と言った
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 無理をした行動
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理をした行動"
		;実は積極			視線を外し、悪くはないよと呟いた
		IF COMCOR_POSI()
			PRINTFORMW 
		;実は嫌				精一杯の強がりで、まあまあ悪くないと答えた
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT分類が受け入れるの場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;ACT派生,5 気持ち良いことしたい
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 5
	;自然な行動
	;=================================================
	;REACT負荷0, もっと気持ち良くなりたいと言った
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 気持ち良いことしたい気分だと答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 媚びるように敢えて淫らな行為をねだった
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;-------------------------------------------------
;以下、上記のCOM、REACT分類に属さないCOM毎の分岐
;-------------------------------------------------
[SKIPSTART]
通常の場合リアクションの地の文は@TRAIN_MESSAGE2と@REACTION_MESSAGEで処理されますが
以下の部分は@TRAIN_MESSAGE2の条件から漏れる為、@REACTION_MESSAGEでのみ処理されます
COM分岐に於いても当然TFLAG:REACT派生は機能しますし、@REACTION_MESSAGE内でもREACT負荷毎に書き分けられています
SELECTCASE GET_COM_RANK()
	;自然な行動
	CASE "自然な行動"
		PRINTFORMW 
	;無理のない行動
	CASE "無理のない行動"
		PRINTFORMW 
	;無理をした行動
	CASE "無理をした行動"
		;実はまんざらでもない
		IF COMCOR_POSI()
			PRINTFORMW 
		;実は嫌
		ELSE
			PRINTFORMW 
		ENDIF
ENDSELECT
上記のような分岐を設ける事によって、本体側の変化に対応する事は可能ですが
テンプレに於いてデフォルトで設けるには手間がかかり過ぎると考える故に、基本的にREACT内のCOM分岐には枠を設けない方針です
汎用的な内容にしてもよし、また部分的に汎用リアクション(COM)に流しても良いと思います
まあREACT負荷以外にも拡張する方法はいくらでもあったりするので、その辺りの判断は各口上作者に委ねたいと思います
3BM/4MBでも書くから兎に角考えられる要素を全部入れてくれ！的な要望がもしあれば、その時はまた考えたいなと
[SKIPEND]

;今の気分を聞くでのCOM沈黙するはTFLAG:REACT派生 == 1(質問に答えない)扱いとなりお仕置きポイントが加算される
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("許しを乞う") && TFLAG:REACT負荷 < 2
	;=================================================
	;お仕置きポイント加算 派生:「意見を出すなんていい度胸だね！」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT0, 今の気分を聞く

;────────────────────────────────────
;REACT1, 自分への気持ちを聞く・個別リアクション
;────────────────────────────────────
;・ACT派生への代入は調教対象のリアクションに応じて@ACTION_APPLY2_1にて行われる
;-------------------------------------------------
@KOJO_REACT_KX1_1
;-------------------------------------------------
;・TFLAG:ACT派生(0=嫌いじゃない…かも知れない…/1=そんなことあるわけない！/2=困るねぇ…好きじゃないといったら怒る？
;・3=もう酷いことしなければ、もっと好きになれるかもしれない/4=はっきりと好きと告げた?5=もっとすごいことしてくれば、もっと好きになれるかも)
;-------------------------------------------------

;REACT分類が消極的従うの場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_0, 「嫌いじゃない…かも知れない…」
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 0
	;自然な行動
	;=================================================
	;REACT負荷0, どうしても嫌いとは言えなかった
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「嫌いじゃない…かも知れない…」と答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 本心を隠すように目を伏せ、「嫌いじゃない…かも知れない…」と答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;リアクションが強気に応答するの場合
;REACT分類は"拒否"にセットされる
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_1, 「そんなことあるわけない！」
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 1
	;自然な行動
	;=================================================
	;REACT負荷0, 「好きになれるわけがない！」と強く答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「そんなことあるわけない！」と言った
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 感情を否定するように首を振り「嫌いだ！」と答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;リアクションが皮肉を言うの場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_2, 皮肉「困るねぇ…好きじゃないといったら怒る？」
;記入チェック
LOCAL = 1
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 2
	;自然な行動
	;=================================================
	;REACT負荷0, 「好きになれるわけがない！」と強く答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「そんなことあるわけない！」と言った
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 感情を否定するように首を振り「嫌いだ！」と答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT分類が積極的従うで、調教対象好意が少ないか、REACT分類が平然と従う
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_3, 「もう酷いことしなければ、もっと好きになれるかもしれない」
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 3
	;自然な行動
	;=================================================
	;REACT負荷0, 「もっと好きになりたいから、酷いことはしないで」と答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「もう酷いことしなければ、好きになれるかもしれない」と答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 機嫌を伺うように視線を向けた後、「酷いことばかりする人は嫌い」と答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT分類が積極的従うで、調教対象好意が1000以上
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_4, はっきり好きだと告げた
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 4
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTREACT(4)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;自然な行動
	;=================================================
	;REACT負荷0, 愛想よく笑って大好きだと答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, はっきりと好きと告げた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 作り笑いを浮かべて、好きだよと答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT分類が受け入れるの場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT1_5, 「もっとすごいことしてくれれば、もっと好きになれるかも」
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 5
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTREACT(5)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;自然な行動
	;=================================================
	;REACT負荷0, 「もっとすごいことしてくれば、もっと好きになれるかも」と答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理のない行動
	;=================================================
	;REACT負荷1, 「もっと好きになるように、もっと可愛がって欲しい」と答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;無理をした行動
	;=================================================
	;REACT負荷2, 「好きになれるくらい、すごいことしてくれればいい」と答えた
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT負荷分岐を全て埋めたら不要な箇所
	;逆に上層の分岐を書かないのであればここで補完させましょう
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;-------------------------------------------------
;以下、上記のCOM、REACT分類に属さないCOM毎の分岐
;-------------------------------------------------

;自分への気持ちを聞くの場合はTFLAG:REACT派生 == 1(質問に答えない)扱いとなりお仕置きポイントが加算される
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;お仕置きポイント加算 派生:「意見を出すなんていい度胸だね！」
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT1, 自分への気持ちを聞く

;────────────────────────────────────
;REACT2, 性についての話をする・個別リアクション
;────────────────────────────────────
;・地の文は@REACTION_MESSAGEで汎用処理される分のみ
;-------------------------------------------------
@KOJO_REACT_KX1_2
;-------------------------------------------------
;　派生する可能性のあるCOMはとりあえずREACT派生分岐を設けていますが
;　&& TFLAG:REACT派生 < 1などとして汎用リアクションで派生を拾う手もあります
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめてと言った
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;お仕置きポイント加算 派生:「意見を出すなんていい度胸だね！」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 気持ち良くして
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("気持ち良くして")
	;=================================================
	;お仕置きポイント加算 「この恥知らずの豚め！」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM10, 恥ずかしがる
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("恥ずかしがる")
	;TEQUIPによって快SOURCEが入っている場合に派生
	;=================================================
	;REACT派生1, 快楽に恥ずかしがる
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT派生0, 状況に恥ずかしがる
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("怯える")
	;=================================================
	;派生：怯えに対する苛立ち
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM50, 自慰し始める
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("自慰し始める")
	;=================================================
	;REACT派生1, 「勝手にオナニーするな」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT派生0, 「勝手にオナニーしてるけどいいか」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT2, 性についての話をする

;────────────────────────────────────
;REACT3, 優しく慰める・個別リアクション
;────────────────────────────────────
;・地の文は@REACTION_MESSAGEで汎用処理される分のみ
;-------------------------------------------------
@KOJO_REACT_KX1_3
;-------------------------------------------------
;　派生する可能性のあるCOMにはREACT派生分岐を設けていますが
;　&& TFLAG:REACT派生 < 1などとして汎用リアクションで派生を拾う手もあります
;-------------------------------------------------
;　とりあえず会話系COMを全て網羅してありますが
;　10番台の愛撫系COM、50番台以降のその他COM等も状況によっては選択可能になります
;　必要に応じて分岐を増減して下さい
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめて
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM2, 無愛想に応答する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("無愛想に応答する")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM3, 愛想良く応答する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("愛想よく応答する")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM4, 強気に応答する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("強気に応答する")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 皮肉を言う
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("皮肉を言う")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, もっとハードにして
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("もっとハードにして")
	;=================================================
	;REACT派生1, 「この恥知らずの豚め！」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT派生0, 「ふーん」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 0
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;REACT派生1, 「意見を出すなんていい度胸だね！」
	;記入チェック
	LOCAL:1 = 1
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM8, 気持ち良くして
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("気持ち良くして")
	;=================================================
	;REACT派生1, 「この恥知らずの豚め！」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT3, 優しく慰める

;────────────────────────────────────
;REACT4, 厳しく脅す・個別リアクション
;────────────────────────────────────
;・
;-------------------------------------------------
@KOJO_REACT_KX1_4
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめて
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 皮肉を言う
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("皮肉を言う")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTREACT(3)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT派生1, 「意見を出すなんていい度胸だね！」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("悲鳴を上げる")
	;TEQUIP:(浣腸＋アナルプラグ/三角木馬)装着中等の場合は痛みのSOURCEが入るので
	;可能性としては考えられますが、この箇所で手間を掛ける必要性としては微妙なところ
	;=================================================
	;REACT派生0, 痛みによる悲鳴
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT派生1, 恐怖による悲鳴
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("怯える") && TFLAG:REACT負荷 < 2
	;=================================================
	;派生：怯えに対する苛立ち
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT4, 厳しく脅す

;────────────────────────────────────
;REACT5, 衣装の変更を要求する・個別リアクション
;────────────────────────────────────
;・調教対象のリアクションによって一枚脱ぐ、二枚脱ぐ、脱がないに分かれる
;・REACT分類が"消極的従う", "受け入れる"の場合は一枚脱ぐ
;・REACT分類が"積極的従う", "平然と従う"の場合は二枚脱ぐ
;・その他の場合は脱がない(PALAM恥情/反抗の数値によって、恥ずかしくて脱がない、嫌がって脱がないに分かれる)
;-------------------------------------------------
@KOJO_REACT_KX1_5
;-------------------------------------------------
;・TFLAG:ACT派生(0=服を脱ぐように命令/1=服を着るように命令)
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_0, 服を脱ぐように命令
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 0
	;=================================================
	;一枚脱いだ(消極的反応)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("消極的従う/受け入れる")
		;-------------------------------------------------
		;最後の一枚を脱いで全裸になった
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && (TEQUIP:MASTER:アクセサリ + TEQUIP:MASTER:靴下 + TEQUIP:MASTER:下半身下着 + TEQUIP:MASTER:上半身下着 + TEQUIP:MASTER:下半身上着 + TEQUIP:MASTER:上半身上着 + TEQUIP:MASTER:全身上着) == 0
			;REACT分類受け入れる			全裸になり、期待と興奮で肌が火照るのを感じた
			IF IS_COMGRONAME("受け入れる")
				PRINTFORMW 
			;REACT分類消極的従う			仕方なく最後の一枚を脱ぎ、全裸になった
			ELSE
				PRINTFORMW 
			ENDIF
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;緩やかな動きで服を一枚脱ぎ捨てた
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && IS_COMGRONAME("受け入れる")
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		;-------------------------------------------------
		;仕方なく服を一枚脱ぎ捨てた
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && IS_COMGRONAME("消極的従う")
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;上層の分岐を全て埋めた場合は不要な箇所
		;逆に埋めない場合はこの箇所が補完の役目を果たします
		;-------------------------------------------------
		;その他・条件なし
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;二枚脱いだ(積極的反応)
	;記入チェック
	LOCAL = 0
	;=================================================
	IF LOCAL && IS_COMGRONAME("積極的従う/平然と従う")
		;-------------------------------------------------
		;全裸になった
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && (TEQUIP:MASTER:アクセサリ + TEQUIP:MASTER:靴下 + TEQUIP:MASTER:下半身下着 + TEQUIP:MASTER:上半身下着 + TEQUIP:MASTER:下半身上着 + TEQUIP:MASTER:上半身上着 + TEQUIP:MASTER:全身上着) == 0
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;積極的に脱ぎ捨てた
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && IS_COMGRONAME("積極的従う")
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;-------------------------------------------------
		;手際よく脱ぎ捨てた
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2 && IS_COMGRONAME("平然と従う")
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
		
		;上層の分岐を全て埋めた場合は不要な箇所
		;逆に埋めない場合はこの箇所が補完の役目を果たします
		;-------------------------------------------------
		;その他・条件なし
		;記入チェック
		LOCAL:2 = 0
		;-------------------------------------------------
		IF LOCAL:2
			PRINTFORMW 
			RETURN LOCAL:2
		ENDIF
	ENDIF
	
	;=================================================
	;脱がなかった
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("拒否")
		;恥ずかしくて脱げなかった
		IF PALAM:MASTER:恥情 > PALAM:MASTER:反抗
			PRINTFORMW 
		;嫌がって脱がなかった
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF


;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT5_1, 服を着るように命令
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && TFLAG:ACT派生 == 1
	;=================================================
	;一枚着た(消極的反応)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL && IS_COMGRONAME("消極的従う/受け入れる")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;二枚着た(積極的反応)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("積極的従う/平然と従う")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;地の文では"嫌がって"という扱い
	;=================================================
	;着なかった
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && IS_COMGRONAME("拒否")
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT5, 衣装の変更を要求する

;────────────────────────────────────
;REACT6, 合意を求める・個別リアクション
;────────────────────────────────────
;・調教対象のリアクションに応じてREACT分類毎に派生する
;-------------------------------------------------
@KOJO_REACT_KX1_6
;-------------------------------------------------
;手間を厭わないのであれば、TALENT:MASTER:オトコ等で内部で分けるのもアリかもしれません
;-------------------------------------------------

;REACT分類が消極的従うの場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_0, 小さい声で了承する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_COMGRONAME("消極的従う")
	;=================================================
	;覚悟を決めたように、小さい声で了承した(自然な行動)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;小さい声で了承した(無理のない行動)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;無理をした行動
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理をした行動"
		;実は積極			恥ずかしさに消え入りそうな声で了承した
		IF COMCOR_POSI()
			PRINTFORMW 
		;実は嫌				悔しさに顔を歪めながらも了承した
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT分類が平然と従うの場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_1, 好きにすればいいと答えた
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_COMGRONAME("平然と従う")
	;=================================================
	;別に嫌じゃないと言った(自然な行動)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;好きにすればいいと答えた(無理のない行動)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;無理をした行動
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理をした行動"
		;実は積極			したいならいいと言った
		IF COMCOR_POSI()
			PRINTFORMW 
		;実は嫌				勝手にすればいいと答えた
		ELSE
			PRINTFORMW 
		ENDIF
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT分類が積極的従うの場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_2, 意を決して捧げてもいいと告げる
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_COMGRONAME("積極的従う")
	;=================================================
	;初めてをあげられて嬉しいと答えた(自然な行動)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;意を決して捧げてもいいと告げた(無理のない行動)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;もう守れないならばせめて優しくされるようにと同意した(無理をした行動)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT分類が受け入れるの場合
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_3, 気持ち良くしてくれるんだったら初めてをあげてもいい
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_COMGRONAME("受け入れる")
	;=================================================
	;初めてを捧げるから気持ち良くして欲しいと言った(自然な行動)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "自然な行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;気持ち良くしてくれるんだったら初めてをあげてもいいと答えた(無理のない行動)
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理のない行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;初めてを捧げろと言うなら、辛さを忘れられるくらい気持ち良くしてと言った
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && GET_COM_RANK() == "無理をした行動"
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;REACT分類がその他の場合(その他のREACT分類の場合、@ACTION_APPLY2_6にて"拒否"にセットされる)
;━━━━━━━━━━━━━━━━━━━━━━━━
;REACT6_4, 合意しない
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_COMGRONAME("拒否")
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTREACT(4)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT6, 合意を求める

;────────────────────────────────────
;REACT7, お仕置きと宣言する・個別リアクション
;────────────────────────────────────
;・宣言してもお仕置きが発生するとは限らない
;-------------------------------------------------
@KOJO_REACT_KX1_7
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;COM"気弱く応答する"は一定条件でREACT分類"許しを乞う"に派生する
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("気弱く応答する") && TFLAG:REACT負荷 < 2
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめてと言った
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM5, 皮肉を言う
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("皮肉を言う")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM6, もっとハードにして
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("もっとハードにして")
	;=================================================
	;REACT派生1, 「この恥知らずの豚め！」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT派生0, 「ふーん」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 0
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("許しを乞う") 
	;=================================================
	;初回
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && FIRSTREACT(7)
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT派生1, 「意見を出すなんていい度胸だね！」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;悲鳴を上げる
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("悲鳴を上げる") && TFLAG:REACT負荷 < 2
	;TEQUIP:(浣腸＋アナルプラグ/三角木馬)装着中等の場合は痛みのSOURCEが入るので
	;可能性としては考えられますが、この箇所で手間を掛ける必要性としては微妙なところ
	;=================================================
	;REACT派生0, 痛みによる悲鳴
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT派生1, 恐怖による悲鳴
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;怯える
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("怯える") && TFLAG:REACT負荷 < 2
	;怯えるでお仕置きポイント加算というのも変な気がしますが、現状はこれが仕様という事で
	;ちなみに@COM7と@COM33は内容がまったく同じだったりします
	;=================================================
	;派生：怯えに対する苛立ち
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT7, お仕置きと宣言する

;────────────────────────────────────
;REACT8, 怒鳴る・個別リアクション
;────────────────────────────────────
;・
;-------------------------------------------------
@KOJO_REACT_KX1_8
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめて
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;REACT派生1, 「意見を出すなんていい度胸だね！」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("悲鳴を上げる") && TFLAG:REACT負荷 < 2
	;TEQUIP:(浣腸＋アナルプラグ/三角木馬)装着中等の場合は痛みのSOURCEが入るので
	;可能性としては考えられますが、この箇所で手間を掛ける必要性としては微妙なところ
	;=================================================
	;REACT派生0, 痛みによる悲鳴
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT派生1, 恐怖による悲鳴
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("怯える") && TFLAG:REACT負荷 < 2
	;=================================================
	;派生：怯えに対する苛立ち
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT8, 怒鳴る

;────────────────────────────────────
;REACT9, 不気味に笑う・個別リアクション
;────────────────────────────────────
;・
;-------------------------------------------------
@KOJO_REACT_KX1_9
;-------------------------------------------------
;━━━━━━━━━━━━━━━━━━━━━━━━
;COM0, 沈黙する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("沈黙する")
	PRINTFORMW 
	RETURN LOCAL
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM1, 気弱く応答する
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("気弱く応答する")
	;REACT分類"許しを乞う"扱い
	;=================================================
	;REACT派生1, もうこんなことやめて
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT分類"消極的従う"扱い
	;=================================================
	;REACT派生0, 気弱く返事した
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM7, 許しを乞う
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("許しを乞う")
	;=================================================
	;REACT派生1, 「意見を出すなんていい度胸だね！」
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0 派生「ふーん」扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM31, 悲鳴を上げる
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("悲鳴を上げる") && TFLAG:REACT負荷 < 2
	;TEQUIP:(浣腸＋アナルプラグ/三角木馬)装着中等の場合は痛みのSOURCEが入るので
	;可能性としては考えられますが、この箇所で手間を掛ける必要性としては微妙なところ
	;=================================================
	;REACT派生0, 痛みによる悲鳴
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 0
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;=================================================
	;REACT派生1, 恐怖による悲鳴
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF

;━━━━━━━━━━━━━━━━━━━━━━━━
;COM33, 怯える
;記入チェック
LOCAL = 0
;━━━━━━━━━━━━━━━━━━━━━━━━
IF LOCAL && IS_NOWCOMNAME("怯える") && TFLAG:REACT負荷 < 2
	;=================================================
	;派生：怯えに対する苛立ち
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1 && TFLAG:REACT派生 == 1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
	
	;REACT派生1分岐を埋めた場合は、事実上TFLAG:REACT派生 == 0(派生「ふーん」)扱い
	;この箇所だけ書く場合はREACT派生に関わらず補完の役目を果たします
	;=================================================
	;その他
	;記入チェック
	LOCAL:1 = 0
	;=================================================
	IF LOCAL:1
		PRINTFORMW 
		RETURN LOCAL:1
	ENDIF
ENDIF
;REACT9, 不気味に笑う